# Rancho Moonrise — Project Context

**Last updated:** 2026-09-05 (rancho-site-daily — **Quiet cruise-control run: site health clean (30/30 sitemap URLs 200), repo clean (HEAD == origin/main, nothing pending to land), Re-Verify Gate 2/2 still_true (`/safari-tents-near-austin/` still unindexed at ~131 days, 5-file NEEDS OWNER set unchanged), no on-page defect found and none manufactured.** Full detail: `run-logs/2026-09-05-seo.md`.)

**Prior entry:** 2026-09-04 (rancho-site-daily — **WeddingWire's `sameAs` contradiction, open since 8/19, is resolved (14 → 15) by varying the query, not retrying the known-403 fetch, and cross-matched to the property's address.** Today's assigned top-autonomous-slot item — "extend AEO measurement to a second answer engine" — closed negative and useful: `WebFetch` on Bing and Google both failed the brand-canary guard identically to 8/17's bare-curl failure, closing the "different fetch mechanism" hypothesis for good — real second-engine AEO measurement needs a paid API key. Logged as MEASUREMENT BLOCKED, not "0 citations," and the shared `client-ops` dead-paths runbook extended with the finding. Apple Maps re-read via a third tool path cross-verifies review-monitor's 8/30 figures (80%, 5 ratings) and surfaces one new item in this task's own remit: listed hours read **"3:00 PM – 5:00 PM daily,"** almost certainly wrong for an events venue — flagged NEEDS ADAM (Apple Business Connect). Everything else unchanged: site health, safari-tents still uncrawled (closed on-site since 8/31), 5-file NEEDS OWNER set untouched. Gate: 4 claims — 1 resolved, 3 still_true. Full detail: `run-logs/2026-09-04-seo.md`.)

**Prior entry (2):** 2026-08-31 (rancho-site-daily — **The 8/25 run is deployed at last, six days after it reported itself shipped — and the safari-tents indexing question is closed on the on-site side.** Two other tasks had flagged the 8/25 tree; this run landed it. **Because the failure being fixed is a run trusting the file it wrote, every verification was run against something other than the working tree:** `git ls-remote` matches local HEAD `818bbed`, `git show origin/main:site/index.html` returns **14** `sameAs` URLs in the pushed blob, and a live `curl` of ranchomoonrise.com returns **14** including the GBP CID and Apple Maps nodes. **One correction to the carried claim: only three of the four 8/25 files were still undeployed.** The fourth — `improvement-plan.html`, carrying the s2 banned-copy template fix — had already reached `origin/main` inside the 8/31 competitive commit `6f306b0`, re-verified in HEAD before writing. **So the highest-risk item in the 8/25 work — the template that would have had the next agent publish banned copy in the answer-first position — was already live before this run started.** The TODO implied it was still exposed; it was not. **The tree was mixed and separating it mattered:** the 11 modified files were not all site-daily's (the 8/25 run-log names exactly four). Committed only this task's workstream, and **recovered 3 orphaned SEO edits that belong to no logged run at all** — making this **at least the second uncommitted site-daily session, not the first**. **Deliberately left 5 files uncommitted** from two other workstreams, including a **44-line deletion in `weddings.html`** stripping the guest-accommodations section and three real photos from a bride-critical page. **Instruction-layer defect fixed:** `CLAUDE.md:25` **and** `AGENTS.md:25` both declared *"Current #1 blocker: DNS cutover … every SEO workstream is dead until this happens"* — resolved **2026-04-30**, four months stale, read by every arriving agent as its second paragraph of orientation. Re-verified live before editing (`server: Vercel`, 9/9 routes 200, TTFB 0.28–0.50s). `AGENTS.md` was **untracked**, so the Codex-side copy was invisible to git. **Same failure family as 8/25 one layer down** — then the instruction layer was primed to publish banned copy, here it declared the workstream dead. **`/safari-tents-near-austin/` closed on-site:** re-verified still absent in the strongest form yet (a topically-exact `site:` query returned 8 owned URLs including `/accommodations/`, titled *"Safari Tents & Cabins"*, and never this page, ~126d) — then **every on-site hypothesis was disconfirmed**: not orphaned (**5 public inbound links, identical to two peers that ARE indexed and ranking**), in sitemap line 145, returns 200, in the blog index, `aeo-block=2`/`speakable=2`, JSON-LD clean. The one promising oddity — this page sits at the **root** while every other post is at `/blog/<slug>/` — was **cleared**: all 7 hrefs, canonical, `og:url`, sitemap and `vercel.json` agree, and `/blog/safari-tents-near-austin/` correctly 404s. **There is no on-site defect; the only remaining lever is Adam's 2-minute URL-Inspection. Do not queue further on-site work against this URL.** A **fifth** on-page lever measured dry, from a new angle. Hygiene: **1.45 GB of untracked binaries ignored, not deleted**. Gate: 9 claims — 2 resolved, 6 still_true, 1 partial. Run-log: `run-logs/2026-08-31-seo.md`.)

**Prior entry (3):** 2026-08-31 (rancho-competitive-weekly — **The August 25 report was written and never published, and the run that wrote it said it had shipped.** Its rewritten `site/competitive-intelligence.md`, its regenerated `site/competitive-intelligence.html`, and its Intel card all stayed on this machine; **the card was never inserted into either improvement-plan file.** The newest card on the dashboard Adam and every downstream agent read was **August 17**, while the markdown had been rewritten twice underneath it. That report's own headline *"Shipped this run — the linked report and the written report are the same document again"* **was never true in production.** This is the same failure family as the WeddingWire error that report surfaced, one layer up: 8/25 caught a claim that entered the record in April and propagated unverified for four months; this one is **a run recording its own output as shipped without checking the destination** — exactly what `memory/feedback_verify_irreversible_at_destination.md` warns against. **A file on disk is a proxy signal; a commit reaching `origin/main` is the destination.** Fixed: recovered 8/25 card + new 8/31 card, **byte-identical in both files** (diffed, 19,214 bytes), markdown and HTML regenerated together, all pushed. While verifying, found and repaired a **pre-existing dual-write drift** — the client-ops mirror was missing the **April 20** card entirely; both files now carry **15 identical cards in identical order**. **Material competitor change: Serana withdrew its published price and repositioned.** The claim carried since May — *"the only tracked competitor publishing a flat full-property buyout price, $1,000/night weekday"* — is dead. Live page now reads *"a flat package rate based on the size of your group and the length of your stay"*: quote-on-request, **Rancho's own model in Serana's words**. Confirmed by **varying the query, not retrying the fetch** — an independent search returned a full repositioning: **21+ boutique wellness retreat**, 53 acres, **up to 20 guests across nine cabins**, spa/gym/yoga studio, second domain `seranaretreats.com`, and it **dropped off the corporate head term** where it held ~#4 six days ago. **Consequence stated rather than smoothed: this removes the entire evidence base for the "publish flat buyout pricing" recommendation** — the one comparable operator who tried it stopped. **The last readable pricing path closed:** ResortPass returned **403/Cloudflare**, the same block review-monitor hit 8/30. Rancho's `$20/$15/4.8★/53` and Lucky Arrow's `$35/4.6★/201` are **HELD, not re-verified, and explicitly not recorded as changed** — an access failure is not a data change, the discipline that vindicated RUN_070. That **freezes this report's single most actionable number**, the 43% day-pass underpricing. **Four blockers now — The Knot, Hotels.com, WeddingWire, ResortPass — all off-domain listings carrying Rancho's own copy/price/reviews, and review-monitor reached the identical "one purchase" conclusion independently on 8/30.** **Rankings:** wedding cluster **reproduced exactly** six days apart (blog ~#3, `/weddings/` ~#4, home ~#6), promoting the 8/25 three-URL finding from new measurement to confirmed; corporate ranch variant held exactly; corporate head term ▲+1. Broad glamping variant ▼ **3 owned URLs → 1** — recorded, but **not** escalated: one query, one read, and the engine's written answer on it still *opens* with Rancho. Net **5 owned URLs across 4 of 11** non-brand queries. The Knot banned copy **confirmed 3rd read**; **first-ever clean brand snippet** (36 acres, 200 guests, no banned copy) — watched, not claimed. **4 new surfaces**, none in `sameAs`: Pop & Drop TX, Rowan & Birch, Well City Guide, Wheree; plus a live **TripAdvisor listing URL** (`d33307272`) recorded for the first time — what `rancho-p1-05` needs to act, with review state deliberately left to review-monitor. Steady: Hipcamp curated **10th absence**, Glamping Hub ~**20wk**, `/safari-tents-near-austin/` ~**126d**. Gate: 16 claims — **2 resolved**, 9 still_true, 1 changed, 1 WATCH, 1 confirmed-3rd, **2 blocked-and-held**. Run-log: `run-logs/2026-08-31-competitive.md`.)

**Prior entry (4):** 2026-08-25 (rancho-site-daily — **The #1 NEEDS ADAM item was never an Adam item, and the instruction layer was primed to publish banned copy.** `sameAs` **11 → 14**. The canonical **Google Business Profile** URL — carried since 8/19 as *"the single highest-value missing entity node, needs one URL from Adam (~30 seconds)"* — was derivable from IDs already on this machine and confirmed live in one fetch. The `fid` in `memory/reference_rancho_gbp.md`, captured from Adam's own `business.google.com` session, **is** the CID: `https://www.google.com/maps?cid=10788924849497903065` returns `<title>Rancho Moonrise - Google Maps</title>`. Two factors — provenance (from inside Adam's own GBP manager) and a live server-side title. **Mechanics worth keeping:** Google Maps serves a JS shell to `WebFetch` *and* to a default `curl` UA, so both read as "no such business"; **only a Googlebot UA renders the title.** A default-UA check here manufactures a confident false negative. **This is the third instance of the same pattern on this property** — 8/19 searched the *repo*, correctly found nothing, then converted "not in the repo" into "only Adam can supply it." The absence was a property of the search scope. Also added: **Apple Maps** (`place-id=IECFC57C438E79EC4`, verified live) — a node no prior run had enumerated, and the entity behind **Siri/Spotlight, an answer surface with zero measurement here**; and **Expedia** (`h89565924`), excluded 8/19 after a 429 plus a wrong-entity search hit. Expedia was resolved **by varying the query, not retrying it** — direct fetch was deliberately *not* attempted (review-monitor's `expedia-direct-fetch` blocker is at 3 consecutive 429s; a 4th proves nothing), and a domain-restricted search returned the real property whose **8.0 rating cross-matches the April anchor tracked via a completely different path**. Review count untouched — that belongs to review-monitor. **WeddingWire still excluded:** 403 on *both* `curl` and `WebFetch`, one read only, and the repo's 68-day-old self-contradiction stands; it becomes the **9th** blocker in a family where **8 name the identical remedy — a rendering scraper. One purchase, re-litigated per-platform for four months.** **Real defect found and fixed:** `site/improvement-plan.html:1076`, inside the **still-open, unstarted** card `rancho-seo-s2-h2-summaries`, carried the propagation template agents are told to copy into the first H2 of four pages — *"a **34-acre** glamping ranch … with **luxury** safari tents"* — two VOICE-GUIDE violations plus a factual error. The 8/18 run found this template and fixed **two** copies (`master-agent.md`, `backlog.md`) and **missed the third — the one rendered on the dashboard Adam reads and agents work from.** The next agent to pick up s2 would have published banned copy in the answer-first position, from an instruction the system itself supplied: same failure mode as the Knot/Hotels.com syndicated description, except self-inflicted and inside our control. `dashboard-state.json`'s "34-acre" strings left alone — they correctly quote *Hipcamp's* violation. **Hygiene:** 258 file-sync duplicates on disk, **0 tracked** (verified before acting — they have never deployed), two inside the deploy root; fixed with a `.gitignore` rule rather than deletion, untracked noise **250+ → 4**, nothing removed from disk. Health: 6 routes + sitemap + robots 200, TTFB 0.249–0.402s, `validate:site` passes, 3/3 index JSON-LD parse, **no public-page copy changed and no visible change**. Gate: 9 claims — **3 resolved**, 3 still_true, 1 fixed-in-run, 2 false/clean. `/safari-tents-near-austin/` **~120d uncrawled (+6)**. **Unchanged strategic read: `sameAs` is an entity/AEO lever and will not move a position** — Glamping Hub (~19 weeks untouched) and the Hipcamp curation gap still outrank every remaining on-site edit. Run-log: `run-logs/2026-08-25-seo.md`.)

**Prior entry (5):** 2026-08-19 (rancho-site-daily — entity graph consolidated `sameAs` 4 → 11; `hotels-com-banned-copy` escalated STALE → **CONFIRMED and re-scoped onto The Knot**; exact-match title/H1 disconfirmed *backwards* (the best-ranking page misses the keyword in its own title). Net read: **ranking tracks SERP fragmentation, not on-page quality**, which promotes Glamping Hub + the Hipcamp curation gap above every remaining on-site edit. Full text append-only in CHANGELOG.md; run-log: `run-logs/2026-08-19-seo.md`.)

---

## What This Is

Advisory engagement for Rancho Moonrise — glamping, events, and retreat ranch on 36 acres, 20 minutes from downtown Austin (street address 20117 Lockwood Rd, 78653 — operational only; never use "Manor" as a location descriptor in copy). Adam runs deal structuring (buyout + new partner) and builds digital/operational systems. Ashley runs day-to-day operations.

**Brand facts (authoritative):** See `VOICE-GUIDE.md` → "Property Facts" section. Never use "luxury", "Hill Country", "Manor", "General Store", or cite a specific unit count. The Neon Moon Barn Lounge is event-only, not a walk-in bar.

**Repo:** `AStyer8345/rancho-moonrise`, branch `main`, deploys to Vercel **Live site:** [ranchomoonrise.com](http://ranchomoonrise.com) (BofillTech hosting — OLD, not editable by Claude) **New site:** rancho-moonrise.vercel.app (Vercel — all dev work happens here) **Improvement plan dashboard:** <https://rancho-moonrise.vercel.app/improvement-plan.html>

---

## Active Blockers

- **Paul/Donna step-away in flight (2026-04-23)** — Paul and Donna emailed that they want to step away; Ben and Robert countered "hand over the books and financials, we're taking control." Donna hasn't replied and is still acting unilaterally (renewing insurance, micromanaging Ashley). Governance conflict in the open until Nancy weighs in. See `meetings/2026-04-23-ashley-onsite.md` in the Cowork workspace for the full record.
- **Beth (bookkeeper) cannot be a QuickBooks channel** — she's a Donna loyalist, reports everything back to the Hirschmans. Even Beth thinks Donna is unreasonable. Need an alternate path to books/financials.
- **Bar manager role unfilled** — effectively full-time (25 distributors, minimums, tracking). Currently on Ashley on top of events/laundry/design/inquiries/two babies. Biggest single operational risk.
- **Inquiry handling is 100% manual** — funnel is \~100 inquiries → 10 replies → 2 tours → 1 booking/day. No CRM, Gmail reminders and copy-paste templates only. Inquiry auto-responder is promoted to the next major build.
- **8 low-res source JPGs cap image quality** — see "Low-res source JPGs" section below. Responsive pipeline can't generate variants bigger than the source; 4 venue photos cap at 1024w and 4 accommodation photos cap at 480w (actually 340×340 thumbnails). Needs re-upload from Ashley's originals.
- ~~DNS cutover not done~~ ✅ **RESOLVED 2026-04-30** — `curl -I ranchomoonrise.com` returns `server: Vercel`. 2026-04-30 repair pass flipped Vercel primary domain to apex, so `www.ranchomoonrise.com` now permanently redirects to `https://ranchomoonrise.com/`. The #1 SEO blocker is unblocked. Next: submit apex sitemap in GSC.
- **Exhibit A missing** — Cannot model buyout without ownership percentages. Cannot go through Beth; need an alternate path.
- **GITHUB_TOKEN on Vercel is broad-scoped** — using `gh auth token` bootstrap. TODO: swap for fine-grained PAT scoped only to `AStyer8345/rancho-moonrise` contents:write.
- ~~GBP access~~ ✅ **UNBLOCKED 2026-04-10** — Adam has Manager access. Tasks #1, #2, #3, #7, #15, #27 now owned by Adam, not Ashley.

## What's Next

- **NEEDS ADAM (GSC — Priority 1, ~2 min — on-site causes now EXHAUSTED 2026-08-31):** **`/corporate-retreats/` is DONE — indexed and ranking ~#6; drop it from the ask.** Only **`/safari-tents-near-austin/`** remains, now **~126 days uncrawled**, re-verified 2026-08-31 in the strongest form yet: a topically-exact `site:ranchomoonrise.com safari tents` query returned **8 owned URLs** — including `/accommodations/`, whose title is literally *"Safari Tents & Cabins"* — and never this page. **This run stopped re-reporting it and diagnosed it instead. Every on-site hypothesis is disconfirmed:** not orphaned (**5 public inbound internal links — identical to `wedding-venues-near-austin` and `corporate-retreat-near-austin`, both indexed AND ranking**), present in `sitemap.xml:145`, returns 200 live, registered in the blog index, `aeo-block=2`/`speakable=2`, JSON-LD clean. The one promising structural oddity — this page sits at the **root** while every other post is at `/blog/<slug>/` — was **tested and cleared**: all 7 inbound hrefs, the canonical, `og:url`, `sitemap.xml` and `vercel.json` all agree on the root form, and `/blog/safari-tents-near-austin/` correctly 404s. **Conclusion: there is no on-site defect. The page is on-page-identical to two peers Google did index. URL Inspection → Request Indexing on that one URL is the only remaining lever. Don't re-submit the sitemap, and do NOT queue further on-site work against this URL** — that is now a closed question, not an open diagnostic. (Indexing owned by `rancho-site-daily` + Adam.)
- **NEEDS OWNER (not site-daily's — flagged 2026-08-31):** **`site/pages/weddings.html` carries an uncommitted 44-line deletion** removing the entire guest-accommodations section and three real photos (`accommodation-cabin`, `accommodation-family-safari`, `accommodation-premium-safari`) from a **bride-critical** page. Deliberately not committed and not reverted — this task cannot tell whether it is intentional. **4 other files** sit uncommitted from two further workstreams: `contact.html` + `api/inquiry.js` (event-budget field + `budget → budget_range` alias) and `site/js/main.js` + `site/css/styles.css` (Flodesk newsletter embed, blog template CSS). Whoever owns these should land or discard them. Also latent: **`api/inquiry.js` contains a NUL byte present in `HEAD` too** — pre-existing, so git classifies the file as binary and hides its diffs from every future reviewer.
- **NEEDS ADAM (Hipcamp strategy — carry-over from 5/16 audit):** audit at `brand/2026-05-16-hipcamp-curation-gap-audit.md` documents 3 structural flags + 8 content gaps that keep Rancho out of Hipcamp's curated editorial set. **6/15 update:** the curated set rotated (broke a 4-week byte-identical streak); Lucky Arrow slipped #8→#9; Rancho still absent (5th read since 5/16). More material this week as aggregators/listicles surged across head terms. **One question for Ashley:** "is the Hipcamp listing intentionally private — SEO presence only — or do we want bookings from it?" Two voice/data fixes worth doing regardless: voice violation ("unwind with a drink at our bar") + acreage drift (overview "34", data field 37, VOICE-GUIDE 36).
- **NEXT COMPETITIVE RUN (2026-09-07) — read before acting on pricing.** (1) **The "publish flat buyout pricing" recommendation is now unsupported** — Serana was its entire evidence base and withdrew its $1,000/night number on 8/31 while repositioning to a 21+ wellness model. Do not re-propose without new evidence. (2) **Day-pass underpricing ($20/4.8★ vs $35/4.6★) is FROZEN, not refuted** — ResortPass went 403/Cloudflare 8/31; both sides are held at 8/25 values and cannot be refreshed until a rendering fetch path exists. (3) **Confirm-or-drop the broad-glamping regression** — 3 owned URLs → 1 on one read; if it reproduces it is real, if not it was query variance. (4) Re-surface the two remaining never-published 8/25 quick wins: **claim the WeddingWire vendor listing** and **fix The Knot description** — `rancho-site-daily` added WeddingWire to `sameAs` 2026-09-04 (resolved by varying the query, not the direct fetch), so that third item is done; these two are still open, never declined, never asked. **WATCH (brand SERP, carried, still not a confirmed regression):** bare-brand query put ranchomoonrise.com ~#8–9 below Do512/Hotels.com/LinkedIn/FB/Hipcamp/Instagram/Yelp; same non-geolocated proxy artifact cleared 6/29. **But the brand snippet itself read clean for the first time** (36 acres, up to 200 guests, no banned copy) — one read, watch, do not claim. **Earned media now 9 surfaces:** Tribeza, Yahoo News, MTHR Collective, Awae Retreats, VenueScanner + **Pop & Drop TX, Rowan & Birch, Well City Guide, Wheree** (none in `sameAs`).
- **NEEDS ADAM (analytics):** provide a GA4 measurement ID (`G-XXXX`) or a GTM container ID (`GTM-XXXX`). The site emits conversion events through `window.rmTrack`; `window.RANCHO_ANALYTICS_CONFIG` is now the single documented placeholder for future GA4/GTM insertion. Without an ID, events still log to `console.debug` for QA but are not reported anywhere.
- **NEEDS ADAM (Apple Business Connect — hours are almost certainly wrong, ~2 min) — added 2026-09-04 by `rancho-site-daily`:** Apple Maps lists Rancho Moonrise's hours as **"3:00 PM – 5:00 PM daily"** (verified live via `maps.apple.com/place?place-id=IECFC57C438E79EC4`). A 2-hour daily window reads like a stale/default value, not real operating hours for an events and glamping venue. This is the entity behind Siri/Spotlight — if either answers "is Rancho Moonrise open" or "what are Rancho Moonrise's hours" from this data, it will tell people the property is closed almost all day, every day. This task has no Apple Business Connect access to fix it directly. **Fix:** open Apple Business Connect (business.apple.com) → correct the listed hours (or mark as "by appointment / event-based" if that's more accurate than a fixed daily window).
- **NEEDS ADAM (Calendly virtual):** create or share a dedicated URL for a real virtual wedding walkthrough booking flow. Until then, virtual CTAs are worded as "Ask about a virtual walkthrough/tour" and fall through to `/contact/?intent=wedding`; add the URL to `CALENDLY_URLS.virtual` in `site/js/main.js` when ready.
- **~~NEEDS ADAM (search API key — Priority 1)~~ → DOWNGRADED TO NICE-TO-HAVE 2026-08-18.** `rancho-seo-s7` is now baselined **without** a key, and SERP verification works fleet-wide **without** one — the harness `WebSearch` tool is the working path; only bare `curl` is bot-detected. A Google Programmable Search Engine key (free, 100 q/day) is still worth having for *certified absolute ranks* and *unattended script runs*, but **nothing is waiting on it**. Do not buy on the original framing below, which is superseded:
  <details><summary>superseded 2026-08-17 wording</summary> `rancho-seo-s7` cannot record a baseline and **no client task can verify a SERP-position claim** — the DuckDuckGo path named in the shared re-verify runbook returns a 202 challenge page, and bare-curl Bing degrades into dictionary results (proven 2026-08-17 by a brand canary that couldn't find the site on its own name). The harness `scripts/serp-baseline.py` is built and waiting on one credential. Cheapest: **Google Programmable Search Engine — free, 100 queries/day**, huge headroom for 10 keywords monthly. Alternatives: Bing Web Search API, SerpAPI, DataForSEO. **⚠️ PARTIALLY CONTRADICTED 2026-08-17 by `rancho-competitive-weekly`:** that task ran **8 live SERP queries the same day** through a different client path and got clean, coherent, cross-verifiable results — including the discovery that Rancho now ranks. The "no client task can verify a SERP-position claim" conclusion is **client-specific, not universal**. Before buying a key, reconcile the two paths: `rancho-seo-s7` may already be unblocked. — **✅ RECONCILED 2026-08-18: it was. One variable — bare curl vs. the harness tool.**
  </details>
- **Next SEO run (2026-09-05+) — read this before queuing another on-page edit or another second-engine attempt.** Four on-page levers are now *measured* and disconfirmed on this property: internal-link depth (8/18 — inverse correlation), word count + schema depth (8/18 — uniform across ranking and non-ranking pages), exact-match title/H1 (8/19 — fails backwards; the best-ranking page misses the keyword in its own title), and whole-site schema coverage (8/16 — 0 invalid JSON-LD, 0 missing `speakable`, `rancho-seo-s4` RESOLVED). **Ranking here correlates with SERP fragmentation, not with on-page quality.** Do not propose on-page passes as default autonomous work — they pass every validator and change nothing. **Closed 2026-09-04: "extend AEO measurement to a second answer engine" via `WebFetch`** — tried Bing and Google directly, both failed the brand-canary guard identically to bare curl (2026-08-17). Not a fetcher-specific artifact; genuine second-engine measurement needs a paid API key (ChatGPT, Perplexity, or Bing Web Search API). **Do not re-attempt via a different fetch mechanism — that hypothesis is now closed.** **Closed 2026-09-04: WeddingWire `sameAs`** — resolved by varying the query, `sameAs` 14 → 15. What is actually left, in order: (1) **re-run the s7 baseline ~2026-09-18** via the harness `WebSearch` tool per `serp-baseline-2026-08-18.md` and diff the table (not due yet); (2) promote `/corporate-retreats/` into main nav — BLOCKED on Adam confirm; (3) if an API key for ChatGPT/Perplexity/Bing is ever provisioned, that unblocks the genuine second-engine AEO baseline. **Highest-leverage items are not on-site at all:** **Glamping Hub submission (free, 15 min, ~19 weeks untouched)** and the **Hipcamp curation gap** outrank every remaining on-site edit, because head terms are owned by aggregators and the only way onto an aggregator's SERP is to be in the aggregator. The rest sits with Adam or Ashley: the blog-pause decision (3 of 4 rankings are blog URLs), the single GSC URL, and the syndicated Knot/OTA description carrying banned copy. **Retired, do not re-propose:** internal-link floor 4 → 5; AggregateRating-on-BlogPosting; exact-match title rewrites; further schema passes. **Closed 2026-08-25:** the GBP `sameAs` ask (derived + verified, never needed Adam) and the Expedia `sameAs` candidate (confirmed by varying the query). Audit Review/AggregateRating numeric anchor stays 125 until Adam confirms the count or supplies a Places API key.
- Adam: Re-upload the 8 low-res source JPGs at 2560px+ width (especially `feature-wedding.jpg` — bride-critical) so the responsive ladder can generate 1920/2560/3840 variants. See "Low-res source JPGs" below.
- The `/api/inquiry` handler POSTs to the CRM webhook directly (`RANCHO_CRM_WEBHOOK_URL` + `RANCHO_API_KEY` already set in Vercel). The CRM-side n8n draft-reply workflow now sends the events@ notification email as a side branch, so Ashley's inbox keeps looking like today's WordPress-form pattern. No Resend setup needed.
- Adam: Curate real-wedding photo set to replace weddings.html gallery placeholder (6 tiles, aria-hidden, "photos coming soon" note live now)
- Adam: Remaining GBP tasks — #3 (answer pending Q&A + seed 10 FAQs), #15 (enable Chat), #27 (upload photos). #1 reviews RESOLVED 2026-04-14; #2 RESOLVED 2026-04-15.
- ~~Adam: DNS cutover from BofillTech to Vercel~~ ✅ **DONE 2026-04-30** — verified live (Vercel server header, 200 on www host, sitemap + landing pages serving from Vercel)
- Adam: Rotate GITHUB_TOKEN on Vercel to a fine-grained PAT (5 min)
- Claude (auto): SEO/AEO prep work on Vercel site — Mon/Wed/Fri at 5 AM
- Claude (auto): Weekly GBP posts — drafts saved to `brand/gbp-posts/` for Adam to review

## Low-res source JPGs (data blocker, not code blocker)

The responsive image pipeline is live and working. But `scripts/generate-responsive-images.sh` refuses to upscale (correctly — cwebp q88 of upscaled pixels just bakes in blur), so the ladder caps at the source dimensions. These 8 files need higher-resolution originals:

FileCurrent sourceLadder generatedWhere it lands`feature-wedding.jpg`1060×651480w, 1024w**weddings.html feature section — bride-critical**`venue-event-barn.jpg`1067×1600 (portrait)480w, 1024whost-your-event.html hero + index split section`about-ranch-aerial.jpg`1600×1067480w, 1024windex.html about split section`feature-safari-tent.jpg`1706×1017480w, 1024windex.html feature split section`accommodation-cabin.jpg`336×338480w only (upscaled)accommodations.html card`accommodation-double-safari.jpg`350×349480w onlyaccommodations.html card`accommodation-family-safari.jpg`339×339480w onlyaccommodations.html card`accommodation-premium-safari.jpg`342×340480w onlyaccommodations.html card

The 4 accommodation files are literally 340×340 thumbnails masquerading as content photos — re-upload priority is HIGH. Re-upload at 1600×1600+ minimum. Once re-uploaded: rerun `./scripts/generate-responsive-images.sh` and commit — no HTML changes needed.

## Last Worked On

- 2026-09-05 (site-daily): **Quiet cruise-control run — no defects found, no on-page work manufactured.** Repo clean (local HEAD == `origin/main`, nothing pending from a prior run); the 5-file NEEDS OWNER uncommitted set unchanged. Site health: sitemap + robots 200, all 30 sitemap URLs individually checked and 200, 6 spot-checked routes 200 at TTFB 0.23–0.60s; PageSpeed API hit a quota wall (same missing-key family as the SERP/Places asks, not a new finding). Re-Verify Gate: brand canary passed, `/safari-tents-near-austin/` re-checked via `site:` query and still unindexed (~131 days, up from ~126 at 8/31) — on-site diagnosis stays closed per the 8/31 finding, no further on-site work queued against it. Checked for a genuine on-page defect (the kind 8/25 and 8/31 each found) before concluding there was none, rather than defaulting to a cosmetic edit that would pass validators and change nothing. Gate: 2 claims, 2 still_true. Run-log: `run-logs/2026-09-05-seo.md`.

- 2026-09-04 (content-weekly): **`events.html` strengthened, 3/4 elements** (3 real previously-orphaned photos — Lodge porch, fire pit circle, barn mural at night — plus an Ashley author block and a local detail on the Event Barn / Neon Moon Barn Lounge day-vs-night split). Testimonial T-001 re-verified still UNAPPROVED (51 days) and doesn't serve this page type regardless. Third consecutive productive content-weekly run. `validate:site` passes, pushed and verified live (`ranchomoonrise.com/events/` confirmed via curl). Full detail: `run-logs/strengthener-queue.md`. Next queue slot: `pool-day-pass-austin.html`.

- 2026-09-04 (site-daily): **WeddingWire's `sameAs` contradiction (open since 8/19) resolved, and today's assigned "extend AEO measurement to a second answer engine" slot closed negative.** WeddingWire added to the entity graph (14 → 15) by varying the query (`Rancho Moonrise weddingwire.com`, independently reconfirmed, cross-matched to the property's operational address) rather than retrying the known-403 direct fetch. Tried `WebFetch` on Bing and Google directly as a genuinely different mechanism than 8/17's bare-curl failure — **both failed the mandatory brand-canary guard identically** (Bing: gorancho.com/Wikipedia instead of the site; Google: error interstitial). Logged as **MEASUREMENT BLOCKED**, not "0 citations", per the shared runbook. Closes the "different fetch path" hypothesis for good — real second-engine AEO measurement needs a paid API key. Extended the shared `client-ops/templates/re-verify-before-report.md` dead-paths table with this finding. Apple Maps re-read via a third tool path cross-verifies review-monitor's 8/30 figures (80%, 5 ratings) and surfaces one new finding: listed hours read **"3:00 PM – 5:00 PM daily"**, almost certainly wrong for an events venue — flagged NEEDS ADAM (Apple Business Connect). Everything else re-verified unchanged (site health, safari-tents still uncrawled, 5-file NEEDS OWNER set untouched). Gate: 4 claims — 1 resolved, 3 still_true. Run-log: `run-logs/2026-09-04-seo.md`.

- 2026-08-25 (site-daily, condensed 2026-09-04): `sameAs` 11 → 14 (derived GBP CID from an existing `fid` rather than waiting on Adam; added Apple Maps + Expedia by varying the query). Fixed a real defect at `improvement-plan.html:1076` — a banned-copy template (**"34-acre… luxury safari tents"**) the next agent would have published verbatim. Full detail: `run-logs/2026-08-25-seo.md`.

- 2026-08-19 (content-weekly, condensed 2026-09-04): Strengthened `host-your-event.html` 3/4 elements with 3 previously-orphaned real photos; testimonial T-001 still blocks the 4th (re-verified UNAPPROVED). Full detail: `run-logs/strengthener-queue.md`.

- 2026-08-31 (site-daily): **The 8/25 run is deployed at last — six days after it reported itself shipped — and the safari-tents indexing question is closed on the on-site side.** Landed the 8/25 tree that `rancho-review-monitor` (8/30) and `rancho-competitive-weekly` (8/31) had both flagged. **Because the failure being fixed is a run trusting the file it wrote, every check ran against something else:** `git ls-remote` matches HEAD `818bbed`, the **pushed blob** carries 14 `sameAs` URLs, and the **live site** serves 14 including the GBP CID and Apple Maps. **Correction to the carried claim — only 3 of the 4 files were still undeployed:** `improvement-plan.html`, with the s2 banned-copy template fix, had already ridden to `origin/main` inside competitive's `6f306b0`, re-verified in HEAD. **The highest-risk item was therefore already live before this run started**, contrary to the TODO. **The tree was mixed:** 11 modified files, but the 8/25 run-log names exactly four — committed only this task's workstream and **recovered 3 orphaned SEO edits belonging to no logged run at all** (speakable ×2, *"Manor"* voice-compliance ×2), making this **at least the second uncommitted site-daily session**. **Left 5 files uncommitted** from two other workstreams, including a **44-line deletion in `weddings.html`** stripping guest accommodations + 3 real photos from a bride-critical page. **Instruction-layer defect killed:** `CLAUDE.md:25` **and** `AGENTS.md:25` both declared *"#1 blocker: DNS cutover — every SEO workstream is dead"* — resolved **4 months ago**; re-verified live before editing (`server: Vercel`, 9/9 routes 200). `AGENTS.md` was **untracked**, so the Codex-side copy was invisible to git. **Same failure family as 8/25 one layer down.** **`/safari-tents-near-austin/` closed on-site:** still absent (~126d) on a topically-exact `site:` query that returned 8 owned URLs including `/accommodations/`, titled *"Safari Tents & Cabins"* — then **every on-site hypothesis disconfirmed**: **5 public inbound links, identical to two peers that ARE indexed and ranking**, in sitemap, 200, in blog index, schema clean. The root-vs-`/blog/` URL oddity was **cleared** (all 7 hrefs, canonical, `og:url`, sitemap and `vercel.json` agree). **No on-site defect exists; the only lever is Adam's 2-min URL-Inspection — do not queue further on-site work against this URL.** A **fifth** on-page lever measured dry. **1.45 GB of untracked binaries ignored, not deleted.** Gate: 9 claims — 2 resolved, 6 still_true, 1 partial. Run-log: `run-logs/2026-08-31-seo.md`.

- 2026-08-31 (competitive-weekly): **The 8/25 report was written and never published — and the run that wrote it said it had shipped.** Its rewritten markdown, its regenerated HTML, and its Intel card all stayed on this machine; **the card was never inserted into either improvement-plan file.** Newest card on the dashboard was **August 17** while the markdown had been rewritten twice underneath it. Same failure family as the WeddingWire error 8/25 itself surfaced, one layer up: **a run recording its own output as shipped without checking the destination.** Fixed — recovered 8/25 card + new 8/31 card, **byte-identical in both files** (diffed, 19,214 bytes), markdown + HTML regenerated together, pushed. Also repaired a **pre-existing mirror drift** (client-ops was missing the **April 20** card); both files now **15 identical cards, identical order**. **Serana withdrew its published price and repositioned** — the May-onward claim "the only tracked competitor publishing a flat buyout price ($1,000/night weekday)" is dead; page now reads "a flat package rate based on the size of your group and the length of your stay", i.e. Rancho's own model. Confirmed by **varying the query, not retrying**: independent search returned a full repositioning to a **21+ boutique wellness retreat**, 53 acres, **9 cabins / up to 20 guests**, second domain `seranaretreats.com`, and it **dropped off the corporate head term** (held ~#4 six days ago). **This removes the entire evidence base for the "publish flat buyout pricing" recommendation.** **ResortPass closed (403/Cloudflare)** — same block review-monitor hit 8/30; Rancho `$20/$15/4.8★/53` and Lucky Arrow `$35/4.6★/201` **HELD, not re-verified, not recorded as changed**, which **freezes the 43% day-pass underpricing finding**. **4 blockers now** (Knot, Hotels.com, WeddingWire, ResortPass) — all off-domain listings carrying Rancho's own copy/price/reviews; review-monitor reached the same "one purchase" conclusion independently 8/30. **Wedding cluster reproduced exactly** (blog ~#3, `/weddings/` ~#4, home ~#6) — new measurement → confirmed; corporate head term ▲+1; broad glamping variant ▼ 3 owned URLs → 1, **not escalated** (one query, one read, and that query's written answer still opens with Rancho). Knot banned copy **3rd read**; **first clean brand snippet ever** — watched, not claimed. 4 new surfaces + live **TripAdvisor URL** (`d33307272`) recorded for the first time. Gate: 16 claims — **2 resolved**, 9 still_true, 1 changed, 1 WATCH, 1 confirmed-3rd, **2 blocked-and-held**. Run-log: `run-logs/2026-08-31-competitive.md`.

- 2026-07-22 → 2026-08-19 (site-daily, condensed): five runs — broke a 39-run cruise-control streak, closed the schema workstream at 100% (`rancho-seo-s4` RESOLVED), measured and resolved `rancho-seo-s7` (4/10 keywords ranking, 4/10 AI citations — 3 of 4 rankings are blog URLs, pipeline paused since 4/23), consolidated `sameAs` 4 → 11, and confirmed `hotels-com-banned-copy` re-scoped onto The Knot. Net read carried forward: ranking tracks SERP fragmentation, not on-page quality. Per-run detail append-only in CHANGELOG.md.

- 2026-08-30 (review-monitor): **RUN_072 — no new reviews anywhere. The last working direct-scrape path on ANY monitored platform closed, and a new readable surface opened the same run.** **ResortPass returned HTTP 403 behind a Cloudflare captcha** — on WebFetch *and* on a desktop-UA `curl`, so site-level bot detection, not a fetcher quirk. RUN_071 had recorded it `direct_fetch_works: true`, *"a WORKING PATH, re-proven"*. Checked against `review-aggregate.json` rather than asserted: google/airbnb/hotels-com (04-17), theknot (05-23), hipcamp (06-03), tripadvisor (07-18), facebook (08-19), expedia (08-21) were already blocked — **ResortPass was the sole survivor. Working direct paths are now ZERO and every held number on this property rests on a search snippet.** The standing "8 blockers, 4 name one remedy" flag is no longer about coverage breadth; direct verification is gone, and the rendering-scraper purchase is now **one purchase against six platforms**. **`53 @ 4.8★` HELD entirely unchanged** — an access failure is not a data change, the discipline that stopped RUN_070 inventing a decline and that RUN_071 vindicated; logged as a **WATCH at 1 of 3**, deliberately below threshold. Third distinct ResortPass failure mode in four runs (extraction → clean → access). **Offsetting find: Apple Maps renders review aggregates** — first read ever, on the node site-daily declared 8/25 as *"the entity behind Siri/Spotlight, an answer surface with zero measurement here"*, which nobody had checked for reviews. Two reads, two URL forms, identical: **80% overall (5 ratings)**. **Not promoted, no held value moved** — attribution to Yelp is **by icon, not stated text**, and Yelp's own domain-restricted result the same run reads **1 review**; five ratings and one review cannot both describe the same pool and the weaker link is the icon. The 80% is **not** recorded as a decline: 80% against three 100% subcategories is internally odd, and a novel well-formed unflattering number about our own property on a first sighting is the exact Travelers' Choice shape. **The Googlebot-UA lead is tried and CLOSED** — site-daily proved 8/25 that only a Googlebot UA renders the Maps CID page, and this task had never tried it (a varied request, not a retry). HTTP 200, title renders, **zero** rating/count tokens in 4,160 bytes: it unlocks **entity identity only, never review data**. Google authoritative **130 now 103 days stale**; contaminated snippet path deliberately not re-run, 2nd consecutive. **Expedia 429 a 4th time → blocker resolution path (d) ("429 is transient, may self-heal") DISCONFIRMED** on the most generous terms available — a 9-day gap with zero requests from this client; no Expedia search issued, deliberately, because count-6 is a **rule** question already with Adam. Held: Facebook 6/86% **5th consecutive**, body still unreadable → **no draft, 5th run, deliberate**; Hipcamp both voice violations **6th consecutive**; Haylee L. 1★ unreplied **day 185 / 26.4 wks**; 2 drafts unposted **day 103**; TripAdvisor 0/unclaimed; Yelp 1 @ 5.0; Airbnb 72nd no-attempt. **Rejected:** the TripAdvisor bleed set recurred ("120 acre", "15 minutes from downtown") **3rd sighting**, plus a `$35–$70` band that arrived inside it and is **not** recorded against the held `$45–$154`. Gate: 19 claims — 7 still_true, **1 REVERSED**, 2 held, 4 blocked/stale, 1 closed_lead, 1 not_requeried, 1 rejected, 1 no_attempt, 1 new, **0 resolved**. Status **urgent** (carried). **6 FLAG_FOR_ADAM.** Run-log: `run-logs/2026-08-30-review-monitor.md`.

- 2026-05-28 → 2026-07-18 (site-daily, condensed): **39 consecutive cruise-control firings, no shipped edits** — gated on a stale GOALS.md pause line, superseded 2026-07-15. Each run still executed its Re-Verify Gate cleanly. Per-run detail append-only in CHANGELOG.md.

---

- 2026-05-19 → 2026-08-21 (review-monitor, condensed): **RUNs 034–071.** **RUN_070 + RUN_071 (8/19, 8/21)** were two quiet sweeps whose findings were all about the measurement apparatus, not the reviews: RUN_070 caught the Google snippet path reading the site's own `reviewCount:"125"` back as a Google count (retiring it as a signal), surfaced Expedia's count at 6 (search-confirmed, not promoted), recovered RUN_069's untracked notes after that run died before writing, and — the one that mattered — **refused to record a missing ResortPass rating block as a decline**, holding `53 @ 4.8★` as an extraction failure. **RUN_071 vindicated that refusal on the record:** the same URL re-rendered at exactly 4.8★ / 53. Had RUN_070 written "no rating found" as a drop, it would have manufactured a second consecutive decline on the one platform carrying this year's only genuine one. RUN_071 also opened the `expedia-direct-fetch` blocker (429 ×3), rejected TripAdvisor's "Travelers' Choice" string as **self-refuting** on its 2nd sighting, and withheld the Expedia count-6 promotion as a **rule** question for Adam. **Earlier: RUNs 034–068.** RUN_068 (8/17) broke a 34-run quiet streak after a 25-day coverage gap and is where **ResortPass** was discovered — 53 reviews @ 4.8★ (+8, −0.1★ since 7/15), Rancho's 2nd-largest review pool, **not in monitored scope**, reply coverage unknown (`/reviews` = 404); and where **Facebook moved for the first time in 48 runs, negatively** (5→6 reviews, 100%→86% recommend, confirmed across three independent queries). Both have since settled at those values. Full RUN_068 text is append-only in `tasks/review-monitor/session-log.md` and `run-logs/2026-08-17-review-monitor.md`. Prior window: **RUNs 034–067 — 34 consecutive quiet sweeps — 34 consecutive quiet sweeps, no new reviews on any platform in 65 days.** (RUN_067 detail — Expedia/Hotels.com split 2nd confirmation + done-log path correction — folded here 2026-08-17, superseded by RUN_068; full text append-only in CHANGELOG.md + session-log.md.) Net state carried forward: Google 130/4.9★ (authoritative 2026-05-19, snippet oscillates 126/175) · Facebook 5/100% · The Knot 8/4.5★ · TripAdvisor 0/unclaimed · Hipcamp 0 · Expedia 8.0. Blockers opened over the window: `google-count`, `hotels-com-direct-fetch`, `airbnb-listing-existence` (all 2026-04-17), `theknot-direct-fetch` (05-23), `hipcamp-direct-fetch` (06-03), `tripadvisor-direct-fetch` (07-18). Two review replies drafted RUN_034 and unposted ever since. Full per-run detail is append-only in `tasks/review-monitor/session-log.md` — nothing lost by collapsing here.

## Mark Done System — How It Works

1. Adam clicks "Mark done" on a task at `/improvement-plan.html`
2. Client POSTs to `/api/complete` with Bearer auth token (stored in localStorage, same secret as client-ops briefing)
3. Serverless function appends a RESOLVED line to `rancho-done-log.md` via GitHub Contents API, commits as `rancho-mark-done` bot
4. Client optimistically moves the task div into `#done-tasks-container` and recomputes grades
5. `rancho-apply-done` scheduled task (daily 5:33 AM local) reconciles log → HTML, physically moving any task divs that are still on the Plan tab into the Done tab container, then commits + pushes
6. Vercel redeploys on push → all devices converge on the same state

---

## Key Metrics (updated April 30, 2026 — week 3 vs. baseline)

| Metric | Value | Delta vs. Apr 9 |
|---|---|---|
| Google reviews | **130 (4.9★)** (live-verified RUN_034 via GBP dashboard; WebSearch snippet still lags at 126) | +5 |
| GBP search impressions | 6,967 (April 14 backfill) | Awaiting next dashboard export |
| GBP profile views | 15,053 (April 14 backfill) | Awaiting next dashboard export |
| GBP clicks | 554 | Flat |
| GBP directions | 513 | Flat |
| GBP calls | 44 | Flat |
| GBP weekly posts | **Yes (Publer auto)** | NEW — 12-event backlog cleared 4/21 |
| Instagram | ~13K followers | Flat |
| Facebook | 864 followers, 5 reviews | Flat |
| TikTok | 1,408 followers | Flat |
| LinkedIn | 106 followers | Flat |
| TripAdvisor | 0 reviews, NOT claimed | Flat |
| Hipcamp | Listed, 0 reviews | Flat |
| Expedia | 8.0 rating | Flat |
| Google ranking (non-brand) | **4 / 10 keywords ranking** (baselined 2026-08-18) | **+4** — first measurement; prior 0/10 carried since April |
| AI engine citations | **4 / 10 cited** (one engine — harness synthesis) | **+4** — ChatGPT/Perplexity/AI Overview still unmeasured |
| Google indexed pages (old site) | ~10 | Flat |
| **DNS cutover** | **✅ Live (4/30)** | NEW — Vercel is now production origin |
| Schema coverage (new site) | **17 / 17 pages** (+`/corporate-retreats/` = 18/18 incl. new landing) | +5 vs. Apr 9 (12/14) |
| Blog individual URLs | **18 posts** | +18 vs. Apr 9 (was 0) |
| Next metrics update | May 7, 2026 | — |
| Improvement tasks done | 3/35 (#4, #6, #10) | Flat |

---

## Property & Deal Summary

- **3 tracts** in same LLC, free and clear (\~$4.2M invested)
- Tract I (9.9 ac) — improved, all operations
- Tract II (11 ac) + III (10.7 ac) — vacant, landlocked
- **Revenue:** 2025 = $350K, 2026 target $500–650K, **Q1 2026 = $61K (on pace)**. Ashley's "safe" mental model is $1M/year.
- Private events 46%, rooms 31%, POS/bar 22%
- **Target event profile:** private party 100+ people, open bar, overnight stays (most profitable configuration per Ashley 2026-04-23)
- **Alcohol:** mandatory through venue (open bar per-person per-hour) as of 2026. Never BYOB. Ordering is a full-time operation (25 distributors).
- **Buyout:** Paul & Donna sent a step-away email 2026-04-23; Ben & Robert countered demanding books/financials; Donna silent but acting unilaterally. Blocked on Exhibit A. Ashley floated a lowball option so remaining members own outright.
- **Christopher:** potential incoming partner, Adam running point. Per Ashley, business plan with realistic projections is the next move to close him.

## People

WhoRoleNancyCo-owner. Deal decision-maker. Needs the Paul/Donna-exit summary.AshleyRuns everything — GBP access, operations. Stretched thin. Target for inquiry-responder voice training.MonetStaff ($28/hr). Target co-owner of shared inquiry inbox.ArlenMaintenance ($170/day)KyliePart-time social ($18/hr)Britney JoGood fit for tours/inquiries per Ashley; has not committedBethBookkeeper (hired via Brian/Paul). **Donna loyalist — do not use as a channel for books/financials.** Even Beth thinks Donna is unreasonable.Paul & Donna (Herchman)Sent step-away email 2026-04-23. Donna still acting unilaterally.Ben & RobertCountered Paul/Donna exit with demand for books. Willing to add capital if needed.ChristopherAdam's potential equity partner. Not yet in; needs business-plan pitch.

## Key Files

- `api/complete.js` — Mark Done serverless function (Vercel, Node 20.x)
- `rancho-done-log.md` — append-only log, source of truth for done state
- `site/improvement-plan.html` — 35 task cards + Plan/Metrics/Audits/Intel/Done tabs
- `brand/2026-04-09-metrics-baseline.md` — live-verified platform data
- `tasks/seo-aeo/` — autonomous SEO/AEO agent workspace
- `site/` — Vercel site (new build, not yet live on main domain)
