# Rancho Moonrise — Project Context

**Last updated:** 2026-08-19 (rancho-site-daily — **The entity graph was the gap, and the banned third-party copy escalated from unverifiable to confirmed-and-spreading.** Sitewide there was exactly **one** `sameAs` array (`index.html:93`) carrying **four social profiles and zero review/booking/OTA profiles** — so ResortPass (53 reviews, 2nd-largest pool), Hipcamp, The Knot, TripAdvisor, Yelp and Hotels.com all sat *outside* the declared entity graph. Shipped: **4 → 11 `sameAs` URLs**, every addition independently verified live this run; **5 deliberately excluded** because they could not be confirmed today (Expedia — 429 + the search pass returned an unrelated "Manor Condominium Resort" page; WeddingWire — repo contradicts itself, the 6/18 audit says no listing exists; Airbnb — room URL not an entity profile; Roadtrippers — not owned; **Google Business Profile — no canonical URL exists anywhere in the repo, and it is the single highest-value missing entry**). Stated plainly: **`sameAs` is not a ranking factor and this will not move a position** — it is a knowledge-graph/AEO lever, correct now that on-page levers are measured dry, but slow and indirect. **The bigger finding:** `hotels-com-banned-copy` — carried as `STALE:2026-06-29`, **unverifiable by direct fetch 4 consecutive runs** — is now **CONFIRMED and mis-scoped**. The sentence *"20 luxury cabins and safari tents for up to 50 guests"* surfaced verbatim across two independent queries, attributed to **The Knot**, not Hotels.com. It is a **syndicated venue description on at least two platforms**, carrying three violations at once: banned word "luxury", banned unit count, and "up to 50 guests" contradicting both the site's `maximumAttendeeCapacity: 200` and The Knot's *own* "300+" claim. **Site-side re-verified clean: 0 matches for "luxur"/"hill country" across index + all 29 pages** — the violation is entirely off-domain and only Adam/Ashley can fix it. **A third on-page lever tested and disconfirmed:** exact-match title/H1. It fails backwards — `wedding-venues-near-austin.html` **misses** the keyword in its own title ("unique wedding venues near Austin") and simultaneously holds the **best position in the whole set (4/8)** on `overnight event venue Austin`, which is not in its title at all. Four pages with exact-match titles rank nowhere. **What the data actually says: ranking correlates with SERP *fragmentation*, not with any on-page attribute** — Rancho ranks where competitors are metafilter/party-boat sites, and is absent wherever aggregators consolidate (Glamping Hub, Hipcamp, Booking, Cvent, EventUp, Eventbrite). **Consequence: on aggregator-owned head terms, on-page work cannot win, because the winners are platforms, not pages** — which promotes the two longest-ignored TODO items (**Glamping Hub submission, ~18 weeks, free, 15 min** and the **Hipcamp curation gap**) above any remaining on-site edit. Health: 6 routes + sitemap 200, TTFB 0.189–0.324s, `validate:site` passes, 2 files +7/-1, no copy and no visible change. Gate: 8 claims — 6 still_true, 1 escalated, 1 fixed-in-run, 0 resolved. Flagged not actioned: ~20 untracked file-sync duplicates, two of them (`weddings 2.html`, `styles 2.css`) inside the deploy root. Run-log: `run-logs/2026-08-19-seo.md`.)

**Prior entry:** 2026-08-18 (rancho-site-daily — **`rancho-seo-s7` is MEASURED and RESOLVED: 4/10 target keywords ranking, 4/10 answer-engine citations.** The slot had been pending since 2026-05-04 and was declared *unmeasurable* by this same task 24 hours earlier. That was wrong, and how it was wrong is the durable finding: **bare `curl` is bot-detected; the harness `WebSearch` tool is not.** Yesterday's run did the hard part right — it caught a false "0/10 ranking" table and refused to fabricate a baseline — then over-generalized from "this script can't measure" to "this client can't measure" and opened a Priority-1 API-key ask. CONTEXT flagged the contradiction (`rancho-competitive-weekly` ran 8 successful SERP queries the same morning) and asked for reconciliation before buying. **Reconciled: one variable, and it was not the credential.** Brand canary passed on the harness path before any absence was recorded. **Ranking URLs:** `/weddings/` on `wedding venue Austin TX ranch` — **the first core landing page ever recorded ranking**; `/blog/corporate-retreat-near-austin/`; `/blog/bachelorette-party-austin-texas/` (5/9, new); `/blog/wedding-venues-near-austin/` (4/8, new, **best position in the set**). **3 of 4 rankings are blog URLs — and the blog pipeline has been PAUSED since 2026-04-23.** Rancho wins specific intent and loses every head term to aggregators; sharpest instance: **ResortPass holds a top-5 slot on `pool day pass Austin TX` while Rancho sells day passes on ResortPass** — reaching that SERP only as a tenant on someone else's domain while its own `/pool-day-pass-austin/` ranks nowhere. **The queued "deepen internal links 4 → 5" work was retired, not performed** — measured correlation is *inverse* (the two best performers have the **fewest** inbound links, 5 each; the most-linked page at 11 doesn't rank), and word count + schema depth are uniform across ranking and non-ranking pages alike. On-page and internal-linking levers on this cluster are **exhausted, now measured rather than asserted**. **Concrete fix shipped:** the AEO entity-authority template in `tasks/seo-aeo/master-agent.md` + `backlog.md` — the exact sentence agents are told to propagate — said **"34-acre"**, contradicting `VOICE-GUIDE.md:271` ("36 acres (not 20, not 31)") and all **183** site instances across 31 files; corrected to 36. The contested "Manor, TX" half deliberately untouched pending Ashley. Site-side acreage drift claim **auto-resolved** — 183/183 read 36, the drift belongs to Hipcamp, not the site. **Cross-repo:** `client-ops/templates/re-verify-before-report.md` now names the harness tool as *the* SERP path fleet-wide. Health: 6 routes + sitemap all 200, TTFB 0.245–0.404s, `validate:site` passes, **no site HTML changed**. Gate: 8 claims — 3 still_true, 1 second-confirmation, **4 resolved**. **4 FLAG_FOR_ADAM:** the API-key ask is **downgraded to nice-to-have** (nothing waits on it); **the blog pause is now measurably expensive and Ashley has never seen the number**; GSC narrowed to the single URL `/safari-tents-near-austin/` (~114d); dead YouTube video day 26. Run-log: `run-logs/2026-08-18-seo.md`.)

**Prior entry (3):** 2026-08-17 (rancho-competitive-weekly — **Rancho is ranking on non-brand commercial keywords for the first time, and the `/corporate-retreats/` indexing blocker is RESOLVED.** Every report since April opened with "0/4 non-brand keywords"; that claim is dead. Three owned URLs now surface, confirmed across three independent live queries plus a `site:` check: `/blog/corporate-retreat-near-austin/` ~**#4** on `corporate retreat near austin texas` and ~#4 on `corporate retreat venue near austin ranch`; `/corporate-retreats/` ~**#6**; `/blog/weekend-getaways-near-austin/` ~**#5** and ~**#4** on two weekend-getaway queries. **`/corporate-retreats/` is indexed** — a `site:` query returns it as top result, closing the #1 Adam-side GSC action carried across 8 consecutive reports. **The critical detail: the blog post outranks the purpose-built landing page** (~#4 vs ~#6), and weekend-getaway is carried entirely by a blog URL — **while the blog pipeline has been paused since 2026-04-23**. The "publish own-domain guide content" lever recommended since May is now demonstrated on this property, not inferred from competitors. **The Re-Verify Gate caught two false prior claims** — Rancho's ResortPass sells **2 products, not 4** (Pilates and Horses $58 + Full Moon Sound Bath $30 are **gone**; 4.9★/45 → **4.8★/53**), and **Lucky Arrow raised day passes $30 → $35 (+17%)**. That yields the sharpest pricing read yet: **Rancho $20 at 4.8★ vs Lucky Arrow $35 at 4.6★ — 43% cheaper at a higher rating**, against a competitor that just proved the market absorbs an increase. **Three new corporate entrants** — Serana (Paige TX, 8 units, **$1,000/night flat full-property**) ~#3, **7744 Ranch** using Rancho's exact "20 min from downtown, just east of Austin" hook, and Artemis Ranch — plus a new content gap: competitors publish buyout pricing, Rancho custom-quotes. 5 claims auto-resolved to done-log. **3 FLAG_FOR_ADAM:** `competitive-intelligence.html` is ~4 months stale behind every Intel-card link; `VOICE-GUIDE.md` contradicts itself on "Manor" (line 270 bans it, line 230 instructs it) on the pages that just started ranking; and **a working SERP path exists** — see the s7 note below. Run-log: `run-logs/2026-08-17-competitive.md`.) 

**Prior entry same day — ⚠️ CONCLUSION OVERTURNED 2026-08-18, condensed:** (rancho-site-daily — attempted `rancho-seo-s7` and concluded it was **unmeasurable from this client**. **That conclusion was wrong and is superseded above** — the failing variable was bare `curl`, not the client. What still stands from that run, and it is the valuable half: a clean-looking "0/10 ranking" table was generated and **killed before it reached this file**, precisely because it matched the known prior state and therefore read as confirmation; the brand canary proved the engine couldn't see the site on its own name (`gorancho.com`, `merriam-webster.com`, `miranchogrill.com` — not `ranchomoonrise.com`); bare-curl DDG HTML/lite both return **HTTP 202 challenge pages**, and bare-curl Bing **degrades into dictionary results** after a few queries. Shipped `scripts/serp-baseline.py` with the canary as a hard gate (`MEASUREMENT BLOCKED`, zero rows, exit 2) — retained, docstring now warns it produces a false negative until a key lands. Also rewrote `tasks/seo-aeo/BLOCKERS.md`, **auto-resolving a 108-day-stale DNS blocker** that still gated "all SEO/AEO work" on a cutover completed 2026-04-30. False alarm correctly caught: `/glamping-near-austin-texas/` 404s but the canonical is `/blog/glamping-near-austin-texas/` (200) — the missing `/blog/` prefix is the recurring trap. Original entry in CHANGELOG.md.)

_(Full 2026-08-17 site-daily detail is in CHANGELOG.md, flagged there as overturned.)_

---

## What This Is

Advisory engagement for Rancho Moonrise — glamping, events, and retreat ranch on 36 acres, 20 minutes from downtown Austin (street address 20117 Lockwood Rd, 78653 — operational only; never use "Manor" as a location descriptor in copy). Adam runs deal structuring (buyout + new partner) and builds digital/operational systems. Ashley runs day-to-day operations.

**Brand facts (authoritative):** See `VOICE-GUIDE.md` → "Property Facts" section. Never use "luxury", "Hill Country", "Manor", "General Store", or cite a specific unit count. The Neon Moon Barn Lounge is event-only, not a walk-in bar.

**Repo:** `AStyer8345/rancho-moonrise`, branch `main`, deploys to Vercel **Live site:** [ranchomoonrise.com](http://ranchomoonrise.com) (BofillTech hosting — OLD, not editable by Claude) **New site:** rancho-moonrise.vercel.app (Vercel — all dev work happens here) **Improvement plan dashboard:** <https://rancho-moonrise.vercel.app/improvement-plan.html>

---

## Active Blockers

- **Paul/Donna step-away in flight (2026-04-23)** — Paul and Donna emailed that they want to step away; Ben and Robert countered "hand over the books and financials, we're taking control." Donna hasn't replied and is still acting unilaterally (renewing insurance, micromanaging Ashley). Governance conflict in the open until Nancy weighs in. See `meetings/2026-04-23-ashley-onsite.md` in the Cowork workspace for the full record.
- **Beth (bookkeeper) cannot be a QuickBooks channel** — she's a Donna loyalist, reports everything back to the Hirschmans. Even Beth thinks Donna is unreasonable. Need an alternate path to books/financials.
- **Bar manager role unfilled** — effectively full-time (25 distributors, minimums, tracking). Currently on Ashley on top of events/laundry/design/inquiries/two babies. Biggest single operational risk.
- **Inquiry handling is 100% manual** — funnel is \~100 inquiries → 10 replies → 2 tours → 1 booking/day. No CRM, Gmail reminders and copy-paste templates only. Inquiry auto-responder is promoted to the next major build.
- **8 low-res source JPGs cap image quality** — see "Low-res source JPGs" section below. Responsive pipeline can't generate variants bigger than the source; 4 venue photos cap at 1024w and 4 accommodation photos cap at 480w (actually 340×340 thumbnails). Needs re-upload from Ashley's originals.
- ~~DNS cutover not done~~ ✅ **RESOLVED 2026-04-30** — `curl -I ranchomoonrise.com` returns `server: Vercel`. 2026-04-30 repair pass flipped Vercel primary domain to apex, so `www.ranchomoonrise.com` now permanently redirects to `https://ranchomoonrise.com/`. The #1 SEO blocker is unblocked. Next: submit apex sitemap in GSC.
- **Exhibit A missing** — Cannot model buyout without ownership percentages. Cannot go through Beth; need an alternate path.
- **GITHUB_TOKEN on Vercel is broad-scoped** — using `gh auth token` bootstrap. TODO: swap for fine-grained PAT scoped only to `AStyer8345/rancho-moonrise` contents:write.
- ~~GBP access~~ ✅ **UNBLOCKED 2026-04-10** — Adam has Manager access. Tasks #1, #2, #3, #7, #15, #27 now owned by Adam, not Ashley.

## What's Next

- **NEEDS ADAM (GSC — Priority 1, ~2 min — NARROWED 2026-08-17):** **`/corporate-retreats/` is DONE — indexed and ranking ~#6; drop it from the ask.** Only **`/safari-tents-near-austin/`** remains, now **~113 days uncrawled** (+33d). A targeted `site:` on that exact path returns `/accommodations/`, `/host-your-event/`, `/weddings/` and blog URLs — never the safari-tents page. Rest of site indexes healthily. URL Inspection → Request Indexing on that one URL. Don't re-submit the whole sitemap. (Indexing owned by `rancho-site-daily` + Adam.)
- **NEEDS ADAM (Hipcamp strategy — carry-over from 5/16 audit):** audit at `brand/2026-05-16-hipcamp-curation-gap-audit.md` documents 3 structural flags + 8 content gaps that keep Rancho out of Hipcamp's curated editorial set. **6/15 update:** the curated set rotated (broke a 4-week byte-identical streak); Lucky Arrow slipped #8→#9; Rancho still absent (5th read since 5/16). More material this week as aggregators/listicles surged across head terms. **One question for Ashley:** "is the Hipcamp listing intentionally private — SEO presence only — or do we want bookings from it?" Two voice/data fixes worth doing regardless: voice violation ("unwind with a drink at our bar") + acreage drift (overview "34", data field 37, VOICE-GUIDE 36).
- **WATCH (brand SERP — recurred 2026-08-17, still not a confirmed regression):** a bare `Rancho Moonrise` query put ranchomoonrise.com ~#9 below Yelp/Do512/Hotels.com/LinkedIn/FB, but a longer brand query put it ~#5 **with four additional owned pages** (summer-glamping, faqs, accommodations, weddings) also surfacing. Same non-geolocated proxy artifact investigated and cleared on 6/29. Confirm via a clean incognito or rank-tracker check before any title-tag/brand-entity work. **New earned media:** Yahoo News video feature + MTHR Collective now join Tribeza on brand queries.
- **NEEDS ADAM (analytics):** provide a GA4 measurement ID (`G-XXXX`) or a GTM container ID (`GTM-XXXX`). The site emits conversion events through `window.rmTrack`; `window.RANCHO_ANALYTICS_CONFIG` is now the single documented placeholder for future GA4/GTM insertion. Without an ID, events still log to `console.debug` for QA but are not reported anywhere.
- **NEEDS ADAM (Calendly virtual):** create or share a dedicated URL for a real virtual wedding walkthrough booking flow. Until then, virtual CTAs are worded as "Ask about a virtual walkthrough/tour" and fall through to `/contact/?intent=wedding`; add the URL to `CALENDLY_URLS.virtual` in `site/js/main.js` when ready.
- **~~NEEDS ADAM (search API key — Priority 1)~~ → DOWNGRADED TO NICE-TO-HAVE 2026-08-18.** `rancho-seo-s7` is now baselined **without** a key, and SERP verification works fleet-wide **without** one — the harness `WebSearch` tool is the working path; only bare `curl` is bot-detected. A Google Programmable Search Engine key (free, 100 q/day) is still worth having for *certified absolute ranks* and *unattended script runs*, but **nothing is waiting on it**. Do not buy on the original framing below, which is superseded:
  <details><summary>superseded 2026-08-17 wording</summary> `rancho-seo-s7` cannot record a baseline and **no client task can verify a SERP-position claim** — the DuckDuckGo path named in the shared re-verify runbook returns a 202 challenge page, and bare-curl Bing degrades into dictionary results (proven 2026-08-17 by a brand canary that couldn't find the site on its own name). The harness `scripts/serp-baseline.py` is built and waiting on one credential. Cheapest: **Google Programmable Search Engine — free, 100 queries/day**, huge headroom for 10 keywords monthly. Alternatives: Bing Web Search API, SerpAPI, DataForSEO. **⚠️ PARTIALLY CONTRADICTED 2026-08-17 by `rancho-competitive-weekly`:** that task ran **8 live SERP queries the same day** through a different client path and got clean, coherent, cross-verifiable results — including the discovery that Rancho now ranks. The "no client task can verify a SERP-position claim" conclusion is **client-specific, not universal**. Before buying a key, reconcile the two paths: `rancho-seo-s7` may already be unblocked. — **✅ RECONCILED 2026-08-18: it was. One variable — bare curl vs. the harness tool.**
  </details>
- **Next SEO run (2026-08-20+) — the runway is thin AND the category has changed. Read this before queuing another on-page edit.** Three on-page levers have now been *measured* and disconfirmed on this property: internal-link depth (8/18 — inverse correlation), word count + schema depth (8/18 — uniform across ranking and non-ranking pages), and exact-match title/H1 (8/19 — fails backwards; the best-ranking page misses the keyword in its own title). **Ranking here correlates with SERP fragmentation, not with on-page quality.** Stop proposing on-page passes as default autonomous work — they will pass every validator and change nothing. What is actually left, in order: (1) **add Google Business Profile to `sameAs`** — the highest-value entity node is still missing and needs one URL from Adam (~30 sec); (2) **re-run the s7 baseline ~2026-09-18** via the harness `WebSearch` tool per `serp-baseline-2026-08-18.md` and diff the table; (3) **extend AEO measurement to a second answer engine** — the 4/10 citation figure still rests on exactly one; (4) resolve the Expedia and WeddingWire `sameAs` candidates (both excluded 8/19 for lack of independent confirmation); (5) promote `/corporate-retreats/` into main nav — BLOCKED on Adam confirm. **Reprioritized upward by the 8/19 evidence:** the two cheapest, longest-ignored items in `TODO.md` — **Glamping Hub submission (free, 15 min, ~18 weeks untouched)** and the **Hipcamp curation gap** — now outrank every remaining on-site edit, because head terms are owned by aggregators and the only way onto an aggregator's SERP is to be in the aggregator. Everything else with real upside sits with Adam or Ashley: the blog-pause decision (3 of 4 rankings are blog URLs), the single GSC URL, and the syndicated OTA/Knot description carrying banned copy. **Retired, do not re-propose:** internal-link floor 4 → 5; AggregateRating-on-BlogPosting. **Superseded prior framing below.** Both remaining mechanical levers are now measured as exhausted: **schema** (whole-site audit 8/16: 0 invalid JSON-LD, 0 pages missing `speakable`, sitemap ↔ routes ↔ files complete in all three directions — `rancho-seo-s4` RESOLVED) and **internal linking** (8/18: inverse correlation — the two best-ranking pages have the *fewest* inbound links; word count and schema depth uniform across ranking and non-ranking pages alike). **Do not run the "deepen 4 → 5 inbound links" pass** — it is low-risk and always available, which is exactly why it kept getting queued, but the evidence points the other way and running it manufactures the appearance of progress. What is actually left: (1) **re-run the s7 baseline monthly** (~2026-09-18) via the harness tool per `serp-baseline-2026-08-18.md` and diff the table; (2) **extend AEO measurement to a second answer engine** — the 4/10 citation figure rests on one; (3) promote `/corporate-retreats/` into main nav (footer-only today) — BLOCKED on Adam confirm. Everything with real upside now sits with Adam or Ashley: the blog-pause decision (3 of 4 rankings are blog URLs), the single GSC URL, and the ResortPass/OTA entity copy. Prior note, still accurate: the schema runway IS exhausted — this time measured whole-site, not cluster-scoped. Today's audit across all 29 public pages: 0 invalid JSON-LD, 0 pages missing `speakable`, and sitemap ↔ routes ↔ page-files complete in all three directions. `rancho-seo-s4` is RESOLVED. Remaining autonomous work is no longer schema-shaped: (1) **AEO baseline (`rancho-seo-s7`) — now the #1 slot.** Query AI engines for the 10 target keywords and establish the tracking baseline; the *measurement* half runs fine regardless of the GSC crawl gate that blocks the rest. (2) Deepen cluster floor 4 → 5 inbound topical links — mechanical, low-risk, same pattern as today's edit. (3) Promote `/corporate-retreats/` into main nav (footer only today) — BLOCKED on Adam confirm. **Retired:** AggregateRating-on-BlogPosting (self-serving review schema earns no SERP stars — off-page GBP is the real lever). **Still deferred:** Person-schema author research (produces a NEEDS ADAM question rather than shipping anything). Adam-side highest-leverage external action unchanged: GSC URL-inspection on `/corporate-retreats/` + `/safari-tents-near-austin/`. Blog pipeline remains PAUSED per Ashley 2026-04-23. Audit Review/AggregateRating numeric anchor stays 125 until Adam confirms the count or supplies a Places API key.
- Adam: Re-upload the 8 low-res source JPGs at 2560px+ width (especially `feature-wedding.jpg` — bride-critical) so the responsive ladder can generate 1920/2560/3840 variants. See "Low-res source JPGs" below.
- The `/api/inquiry` handler POSTs to the CRM webhook directly (`RANCHO_CRM_WEBHOOK_URL` + `RANCHO_API_KEY` already set in Vercel). The CRM-side n8n draft-reply workflow now sends the events@ notification email as a side branch, so Ashley's inbox keeps looking like today's WordPress-form pattern. No Resend setup needed.
- Adam: Curate real-wedding photo set to replace weddings.html gallery placeholder (6 tiles, aria-hidden, "photos coming soon" note live now)
- Adam: Remaining GBP tasks — #3 (answer pending Q&A + seed 10 FAQs), #15 (enable Chat), #27 (upload photos). #1 reviews RESOLVED 2026-04-14; #2 RESOLVED 2026-04-15.
- ~~Adam: DNS cutover from BofillTech to Vercel~~ ✅ **DONE 2026-04-30** — verified live (Vercel server header, 200 on www host, sitemap + landing pages serving from Vercel)
- Adam: Rotate GITHUB_TOKEN on Vercel to a fine-grained PAT (5 min)
- Claude (auto): SEO/AEO prep work on Vercel site — Mon/Wed/Fri at 5 AM
- Claude (auto): Weekly GBP posts — drafts saved to `brand/gbp-posts/` for Adam to review

## Low-res source JPGs (data blocker, not code blocker)

The responsive image pipeline is live and working. But `scripts/generate-responsive-images.sh` refuses to upscale (correctly — cwebp q88 of upscaled pixels just bakes in blur), so the ladder caps at the source dimensions. These 8 files need higher-resolution originals:

FileCurrent sourceLadder generatedWhere it lands`feature-wedding.jpg`1060×651480w, 1024w**weddings.html feature section — bride-critical**`venue-event-barn.jpg`1067×1600 (portrait)480w, 1024whost-your-event.html hero + index split section`about-ranch-aerial.jpg`1600×1067480w, 1024windex.html about split section`feature-safari-tent.jpg`1706×1017480w, 1024windex.html feature split section`accommodation-cabin.jpg`336×338480w only (upscaled)accommodations.html card`accommodation-double-safari.jpg`350×349480w onlyaccommodations.html card`accommodation-family-safari.jpg`339×339480w onlyaccommodations.html card`accommodation-premium-safari.jpg`342×340480w onlyaccommodations.html card

The 4 accommodation files are literally 340×340 thumbnails masquerading as content photos — re-upload priority is HIGH. Re-upload at 1600×1600+ minimum. Once re-uploaded: rerun `./scripts/generate-responsive-images.sh` and commit — no HTML changes needed.

## Last Worked On

- 2026-08-19 (content-weekly): **`host-your-event.html` strengthened — 3 of 4 elements; second consecutive productive run after the 10-run/91-day block.** 3 real photos placed, all previously **orphaned** (present in the responsive ladder, referenced nowhere in the repo): `event-fringe-pergola` replaced a **duplicate** `venue-event-barn` that was rendering twice on this page; `event-picnic-thunderbird` + `event-outdoor-bar-disco` went into two new splits. Every alt text written after opening the file — none inferred from a filename. Second author block on the site (`Ashley · Rancho Moonrise`), plus `author` Person and `dateModified` added to a `WebPage` block that carried **neither**. Local detail: the thunderbird mural / *BE HERE NOW* / breeze-block courtyard read straight off the photo, plus the **Travis County burn ban** constraint (county-specific, so uncopyable). **Testimonial still blocks the 4th element — re-verified, not assumed:** T-001 is `UNAPPROVED` / `EVENT DATE: UNKNOWN` at day **35**, and `host-your-event.html` is the exact page its own file nominates for it, so this run would otherwise have hit **4 of 4**. **Cut deliberately:** the 9:30 PM curfew — ideal as an uncopyable planning detail — contradicts this page's own live "celebrations that go late"; Ashley's call, logged to TODO. **Left alone:** capacity reads 200 in the AEO block, 1,000+ in the FAQ, 200 in schema — a three-way contradiction predating this run, same family as today's Knot "50 vs 300+" finding. `validate:site` passes; 4/4 JSON-LD parse; 6/6 assets decode; 0 banned words, 0 emoji. **No screenshot — dev servers cannot start in an unattended scheduled run**, so the portrait-into-4:3 centre-crop is reasoned, not seen. 2 files, +49/−1 plus sitemap. Zero new URLs. Run-log: `run-logs/strengthener-queue.md`.

- 2026-08-19 (site-daily): **Entity-graph consolidation shipped (`sameAs` 4 → 11); banned third-party copy escalated STALE → CONFIRMED and re-scoped off Hotels.com.** The site had exactly one `sameAs` array sitewide, listing four social profiles and **zero** review/booking/OTA profiles — every surface carrying an actual review pool sat outside the declared entity graph. Added 7 independently-verified URLs (YouTube, ResortPass, Hipcamp, The Knot, TripAdvisor, Yelp, Hotels.com); **excluded 5** that could not be confirmed today, including **Google Business Profile — no canonical URL exists in the repo at all**, now the highest-value one-line ask. Said plainly in the run-log: this is an AEO/knowledge-graph lever, **not a ranking factor**, and will not move a position. Separately, `hotels-com-banned-copy` — unverifiable by direct fetch 4 consecutive runs — is now **confirmed live on The Knot**, not (only) Hotels.com: *"20 luxury cabins…up to 50 guests"*, three violations in one sentence, contradicting both the site's `maximumAttendeeCapacity: 200` and The Knot's own "300+". Site itself re-verified clean (0 "luxury"/"Hill Country" across 30 pages). **Third on-page lever disconfirmed** (exact-match title/H1) — the best-ranking page misses the keyword in its own title. Net strategic read: ranking tracks SERP fragmentation, not on-page quality, which promotes Glamping Hub + Hipcamp above every remaining on-site edit. 2 files, +7/-1, `validate:site` passes. Gate: 8 claims, 6 still_true, 1 escalated, 1 fixed, 0 resolved. Run-log: `run-logs/2026-08-19-seo.md`.

- 2026-08-18 (site-daily): **`rancho-seo-s7` MEASURED and RESOLVED — 4/10 keywords ranking, 4/10 answer-engine citations.** Overturned yesterday's "unmeasurable from this client" conclusion in one query: bare `curl` is bot-detected, the harness `WebSearch` tool is not. Brand canary passed before any absence was recorded. 3 of 4 rankings are blog URLs (pipeline paused since 4/23); `/weddings/` is the first core landing page ever recorded ranking. **Retired rather than performed** the queued 4 → 5 internal-link pass — measured correlation is inverse. Fixed a real defect in the AEO entity-authority template (**34-acre → 36**) that agents are instructed to propagate; site itself verified clean at 183/183. Cross-repo fix to the shared re-verify runbook names the working SERP path fleet-wide. No site HTML changed. Gate: 8 claims, 4 resolved. Run-log: `run-logs/2026-08-18-seo.md`.

- 2026-08-17 (site-daily) — ⚠️ **conclusion overturned 2026-08-18:** **`rancho-seo-s7` attempted and proven unmeasurable; harness shipped that cannot fabricate a baseline.** All SERP paths dead/unreliable (DDG 202 challenge ×2; Bing degrades to dictionary results). A clean-looking "0/10 ranking" table was generated and **killed before reporting** — it matched the known prior state exactly, which is precisely what made it dangerous. Brand canary proved the engine couldn't see the site on `Rancho Moonrise` itself. Shipped `scripts/serp-baseline.py` (canary as hard gate → `MEASUREMENT BLOCKED`, 0 rows, exit 2). Rewrote `tasks/seo-aeo/BLOCKERS.md`, auto-resolving a **108-day-stale DNS blocker** that gated the whole workstream. Cross-repo: fixed the shared client-ops re-verify runbook, which named the now-dead DDG path as *the* SERP verification path fleet-wide. No site HTML changed. Run-log: `run-logs/2026-08-17-seo.md`.

- 2026-08-16 (site-daily): **Schema workstream closed at 100% — `rancho-seo-s4` RESOLVED; blog-cluster link floor restored to uniform 4.** 23-day coverage gap (no run 7/24 → 8/16). Pre-scoped next-slot queue turned out already shipped by commits `089439f` (7/23, videos.html) + `531d7a5` (7/24, accessibility + policies) — re-verify gate caught it before duplicate work. Sitewide audit of 29 pages: **0 invalid JSON-LD, 0 without speakable**; sitemap/routes/page-files complete in all three directions. Shipped one `<li>` in `bachelorette-party-austin-texas.html` → `/blog/ranch-wedding-texas/`, which was the lone post at 3 inbound vs a floor of 4 (verified against HEAD: a long-standing gap, **not** a regression). Anchor variance across existing inbound links deliberately preserved. `validate:site` passes. FLAG: dead YouTube video on `/videos/` day 24. Run-log: `run-logs/2026-08-16-seo.md`.

- 2026-08-21 (review-monitor): **RUN_071 — no new reviews anywhere, and the headline is a RESOLUTION rather than a discovery: the Re-Verify Gate completed its first full save-and-recover cycle on this property.** RUN_070's ResortPass fetch returned HTTP 200 with products, star class and pool copy rendering normally but **no rating and no count surviving**; it classified that as an *extraction failure* and held `53 @ 4.8★` `STALE` rather than recording a decline. **Today the same URL returned 4.8★ / 53 / $15 / $20 — identical.** The refusal is vindicated on the record. That mattered more than it looks: ResortPass is the one platform carrying this year's only genuine rating decline (4.9→4.8 across +8, RUN_068), so a second consecutive drop would have *fit the story perfectly* — which is exactly what would have made it dangerous. Value now rests on **four independent reads**; `stale` cleared, failure count 1→0 (never hit the 3-run threshold), ResortPass off the stale list. **Two opposite things happened on Expedia in the same run.** Direct fetch returned **HTTP 429 a 3rd consecutive time → new blocker `expedia-direct-fetch`** opened (tracked separately from `hotels-com-direct-fetch`: different entity, different failure mode). Simultaneously **the count of 6 earned its 3rd independent, 3rd-phrasing, domain-restricted confirmation** — *exactly* the precondition RUN_070 set for promoting it into the authoritative field. **The precondition is met and the promotion is withheld**, because the hard rule demands a direct scrape and the direct scrape just became a blocker: *"pending a direct scrape"* now means *"pending forever."* That is a **rule** question, not a data question, so it is escalated rather than taken unilaterally (same precedent as the 3-run-old ResortPass scope change). The RUN_070 coincidence flag is de-risked without being smoothed — Facebook's 6 came from a `facebook.com`-restricted query, Expedia's from an `expedia.com`-restricted one; two domains, independently sourced, still a genuine coincidence. **Refused this run:** TripAdvisor's *"Travelers' Choice award … top 10% of properties"* string **RESURFACED — 2nd sighting** (RUN_061, RUN_062-transient) and was rejected as **self-refuting** — the award is defined by consistently great reviews and this listing has **zero** and is unclaimed; now recorded as a *recurring* artifact so future runs reject on sight, because flattering well-formed news about your own property is the finding nobody's instinct challenges. Also rejected: *"15 minutes from downtown Austin"* (Rancho is 20) from the same bleed set. **The OTA split was NOT re-confirmed and the counter was deliberately NOT incremented** — both listings live, but neither 9.0 nor 8.6 appeared inline; a listing being live is not a rating being read. **The Google snippet query was deliberately not run** — RUN_070 proved it echoes the site's own `reviewCount:"125"`, so re-issuing it would re-inject a self-referential number as a fresh read; authoritative 130 is now **94 days stale**. Held: **Facebook 6/86% 4th consecutive confirmation** (1st domain-restricted) with text still unreadable → **no draft, 4th run, deliberate**; **Hipcamp's two voice violations 5th consecutive** (the 34-acre drift is Hipcamp's alone, site clean 183/183); Haylee L. 1★ unreplied **day 176**; 2 drafts unposted **day 94**; TripAdvisor 0/unclaimed; Airbnb 71st no-attempt. Gate: 19 claims — 12 still_true, **1 resolved**, 1 not-reconfirmed, 1 rejected-artifact, 2 unknown/blocked, 1 verification-failure, 1 no-attempt. Status **urgent** (carried). **5 FLAG_FOR_ADAM**, incl. the new one: **8 open blockers and 7 are the same problem** — four name the identical remedy (a rendering scraper), which is one purchase, not four workarounds. Run-log: `run-logs/2026-08-21-review-monitor.md`.

- 2026-08-19 (review-monitor, condensed): **RUN_070 — quiet sweep; three findings all about the measurement apparatus, not the reviews.** Expedia's count surfaced at 6 for the first time since the April baseline (search-confirmed ×2, not promoted); the Google snippet path was caught reading the site's own `reviewCount:"125"` back as a Google count, retiring it as a signal; and RUN_069 was found to have scraped every platform and died before writing, its raw notes untracked in git — recovered and folded in. Also: the ResortPass rating block failed to render and was correctly held rather than recorded as a drop — **vindicated by RUN_071 above**. Full text append-only in `tasks/review-monitor/session-log.md` + `run-logs/2026-08-19-review-monitor.md`.

- 2026-07-22 (site-daily): **Cruise-control streak broken at 39 — first shipped edit since 5/26.** Pause gate was stale: GOALS.md (mtime 7/20) narrowed the Rancho pause to *outreach only* and marked site/content work RESUMED 2026-07-15; the 7/17 + 7/18 runs idled through an unblocked workstream. Shipped `WebPage`/`SpeakableSpecification` + `FAQPage` on `/blog/weekend-getaways-near-austin/` — the only blog-cluster page with a full visible `.aeo-block` Quick-Answers section (4 Q&As) and no schema exposing it. FAQ text extracted from the DOM and asserted verbatim, never authored. 2 files, +58/-2, schema-only. Cluster 16/17 → 17/17 on both types; `validate:site` passes; 0 invalid JSON-LD sitewide. 3 stale claims auto-resolved to done-log (NEEDS ADAM #0 pause-list drift closed after 56 days; AggregateRating-on-BlogPosting retired; s4 → PROGRESS 90%). Run-log: `run-logs/2026-07-22-seo.md`.

- 2026-06-29 (competitive-weekly): **June 29 intel run** — Intel card dual-written byte-identical to both improvement-plan files (verified via diff), `competitive-intelligence.md` rewritten. Two-week gap (June 22 didn't fire). Re-Verify Gate 11 claims: 7 still_true, 3 routine SERP shifts, 1 genuine competitor change (Spoon Mountain price ~$245→$300–500 + romantic page moved, old URL 404s), 1 UNVERIFIED-on-page (Hotels.com fetch blocked, snippet-confirmed 7th wk), **2 auto-resolved → done-log** (brand-SERP ~#7 watch item → owned site is #1 live; Spoon Mountain old romantic URL → 404/relocated). **Both 6/15 lead claims reversed:** aggregator surge was a one-week artifact (property domains reclaimed top — Udoscape/SFTS/Spoon Mountain all #1); brand-SERP #1 confirmed healthy. Corporate cluster churned (Sage Hill #1; Crystal Creek/Lucky Arrow re-entered; Yurtopian off). Structural gaps stable (Glamping Hub ~11wk, Hipcamp 6th read no rotation). No subject-property edits (Rancho paused). Run-log: `run-logs/2026-06-29-competitive.md`.

- 2026-05-28 → 2026-07-18 (site-daily, condensed): **39 consecutive cruise-control firings, no shipped edits.** All gated on a GOALS.md line — "No Rancho Moonrise active work — paused" — that was superseded on 2026-07-15 when the pause was narrowed to *outreach only*. The last ~3 runs idled through an already-unblocked workstream. Each run did execute its Re-Verify Gate (live URL + schema spot-checks, consistently 6–9/9 still_true, 0 resolved) and wrote a dated `run-logs/YYYY-MM-DD-seo.md`; per-run detail lives in CHANGELOG.md (append-only archive). Collapsed here 2026-07-22 to bring CONTEXT.md back under the 150-line cap per CLAUDE.md.

- 2026-05-11 → 2026-06-08 (competitive-weekly, condensed): Four intel runs. Net carry-forward: Rancho 0/5 non-brand keywords, #1 on brand; `/corporate-retreats/` + `/safari-tents-near-austin/` persistently uncrawled. Material findings that still matter: **live ResortPass listing discovered 5/18** (dormant then; activated by 7/15); Google began indexing the apex 5/18 (reframed GSC work from "site not crawled" to "request indexing on specific URLs"); **Hipcamp curation gap** — Rancho absent from the curated "20 Best Glamping Near Austin" set across every read, Lucky Arrow entered it 6/1; Hotels.com third-party listing carries VOICE-GUIDE-banned copy. Per-run detail is append-only in CHANGELOG.md.

- 2026-05-17 → 2026-05-25 (SEO daily, condensed — five-slot CreativeWork-property enrichment workstream on BlogPosting JSON-LD cluster, 17 posts): 5/17 `wordCount` (range 1,005–1,993, median ~1,256, total 22,083; no post below 1k-word thin-content floor). 5/21 `articleSection` (six buckets: Things to Do 6, Corporate Retreats 3, Glamping 3, Bachelorette 2, Weddings 2, Events 1). 5/23 `image` URL string → `ImageObject` with `width`/`height` read live via `sips` (5 distinct images back the cluster: `og-image.jpg` 2134×1412 ×13 posts, `feature-safari-tent.webp` 800×477, `wedding-hero.webp` 1600×1066 ×2, `blog-corporate-retreat.jpg` 6192×4128 ×2, `blog-manor-things-to-do.webp` 800×496). 5/24 `inLanguage: "en-US"` (BCP 47 region tag for international AI engines, Bing in particular). 5/25 `isAccessibleForFree: true` (defends against AI-engine paywall mis-classification; mirrors NYT/WaPo/Reuters open-content patterns). Same pattern across all five: surgical regex pass, `BlogPosting.dateModified` bumped, sitemap synced 1:1, `npm run validate:site` passes, all 17 BlogPosting blocks parse-verified per slot, pre-existing prior-session uncommitted changes (styles.css/main.js/weddings.html/contact.html/api/inquiry.js) intentionally NOT staged (5/7– convention), Re-Verify Gate 6/6 still_true 0 resolved per run. None of the five maps to a named SEO task ID (s1–s8) — all are post-level CreativeWork-property additions. 5/25 audit caught a stale CONTEXT claim: `isAccessibleForFree` already on 1 file but that file was `corporate-retreats.html` EventVenue (not a BlogPosting) — "0/17 BlogPosting carry it" re-confirmed still_true via second-pass parse.

- 2026-05-15 → 2026-05-17 (SEO daily, condensed): 5/15 `ItemList` enrichment on `/blog/things-to-do-near-austin-with-kids/` closing the 3-post ItemList rollout (5/12 weekend-getaways + 5/13 manor-tx + 5/15 with-kids); 5/16 Hipcamp curation-gap audit shipped as research-only deliverable at `brand/2026-05-16-hipcamp-curation-gap-audit.md` (3 structural exclusion flags + 8 content-quality gaps; strategic Ashley question raised not answered); 5/17 `wordCount` enrichment on 17-post BlogPosting cluster (range 1,005–1,993, median ~1,256, total 22,083 — no post below 1k-word thin-content floor). Same surgical pattern: schema-only, dateModified bumped, sitemap synced, validate:site passes, prior-session changes intentionally not staged, Re-Verify Gate clean.

- 2026-05-07 → 2026-05-10 (SEO daily, condensed): 5/7 AEO answer-first blocks added on `/faqs/` + `/accommodations/` (speakable selectors extended); 5/8 FAQPage parity closure on `/faqs/` (17 → 18 Q&A); 5/9 internal-linking pass — 4 critically-orphaned blog posts brought 1 → 4 inbound topical links via 12 `<li>` inserts; 5/10 internal-linking close-out — 3 lightly-linked posts brought 2 → 4 inbound via 6 `<li>` inserts across 5 host pages. Combined effect: zero blog posts below 4 inbound topical links — entire cluster at clean baseline.

- 2026-05-01 → 2026-05-06 (condensed): /corporate-retreats/ landing page shipped (5/1, EventVenue + BreadcrumbList + FAQPage + Speakable, 4 JSON-LD); 7-inbound internal-linking pass into corporate cluster (5/2); AggregateRating closure on /events/ → coverage 17/17 (5/3); meta-description CTR sweep on 18 pages within Google's 160-char cutoff (5/4); title-tag CTR sweep on 10 pages within ~60-char cutoff (5/5); image alt-text audit + 9 Instagram-grid alts populated + 3 long alts trimmed (5/6); 5/4 competitive-weekly logged Lucky Arrow ResortPass window extended past projected 5/1 reactivation, Green Acres ATX as new SERP entrant, Walden Retreats unit-count tightening.

- 2026-04-30 (DNS cutover + adversarial site repair + AggregateRating utility-page closure, condensed): Canonical apex + 308 redirect + sitemap rewrite gaps closed; analytics placeholder + `npm run validate:site` shipped; DNS verified live (Vercel server header, x-fw-server gone); AggregateRating closed on contact + faqs (16/17). All outputs baked into current site state.

- 2026-04-21 → 2026-04-30 (condensed): AggregateRating 5 → 16/17 over 5 runs; FAQPage on safari-tents (4/26); events rotated to July (4/27); blogs #15–18; ReservePage on pool-day-pass-austin; admin/galleries CRUD + Publer 12-event backlog cleared (4/21); Ashley onsite 4/23 (Paul/Donna step-away, alcohol mandatory through venue, blog pipeline PAUSED, voice guide pricing frame added). 4/27 competitive-weekly: Retreat on the Hill (11 multi-format), Spoon Mountain private pools, Yurtopian corp-retreats, DNS still Flywheel pre-cutover.

---

- 2026-05-19 → 2026-08-17 (review-monitor, condensed): **RUNs 034–068.** RUN_068 (8/17) broke a 34-run quiet streak after a 25-day coverage gap and is where **ResortPass** was discovered — 53 reviews @ 4.8★ (+8, −0.1★ since 7/15), Rancho's 2nd-largest review pool, **not in monitored scope**, reply coverage unknown (`/reviews` = 404); and where **Facebook moved for the first time in 48 runs, negatively** (5→6 reviews, 100%→86% recommend, confirmed across three independent queries). Both have since settled at those values. Full RUN_068 text is append-only in `tasks/review-monitor/session-log.md` and `run-logs/2026-08-17-review-monitor.md`. Prior window: **RUNs 034–067 — 34 consecutive quiet sweeps — 34 consecutive quiet sweeps, no new reviews on any platform in 65 days.** (RUN_067 detail — Expedia/Hotels.com split 2nd confirmation + done-log path correction — folded here 2026-08-17, superseded by RUN_068; full text append-only in CHANGELOG.md + session-log.md.) Net state carried forward: Google 130/4.9★ (authoritative 2026-05-19, snippet oscillates 126/175) · Facebook 5/100% · The Knot 8/4.5★ · TripAdvisor 0/unclaimed · Hipcamp 0 · Expedia 8.0. Blockers opened over the window: `google-count`, `hotels-com-direct-fetch`, `airbnb-listing-existence` (all 2026-04-17), `theknot-direct-fetch` (05-23), `hipcamp-direct-fetch` (06-03), `tripadvisor-direct-fetch` (07-18). Two review replies drafted RUN_034 and unposted ever since. Full per-run detail is append-only in `tasks/review-monitor/session-log.md` — nothing lost by collapsing here.

## Mark Done System — How It Works

1. Adam clicks "Mark done" on a task at `/improvement-plan.html`
2. Client POSTs to `/api/complete` with Bearer auth token (stored in localStorage, same secret as client-ops briefing)
3. Serverless function appends a RESOLVED line to `rancho-done-log.md` via GitHub Contents API, commits as `rancho-mark-done` bot
4. Client optimistically moves the task div into `#done-tasks-container` and recomputes grades
5. `rancho-apply-done` scheduled task (daily 5:33 AM local) reconciles log → HTML, physically moving any task divs that are still on the Plan tab into the Done tab container, then commits + pushes
6. Vercel redeploys on push → all devices converge on the same state

---

## Key Metrics (updated April 30, 2026 — week 3 vs. baseline)

| Metric | Value | Delta vs. Apr 9 |
|---|---|---|
| Google reviews | **130 (4.9★)** (live-verified RUN_034 via GBP dashboard; WebSearch snippet still lags at 126) | +5 |
| GBP search impressions | 6,967 (April 14 backfill) | Awaiting next dashboard export |
| GBP profile views | 15,053 (April 14 backfill) | Awaiting next dashboard export |
| GBP clicks | 554 | Flat |
| GBP directions | 513 | Flat |
| GBP calls | 44 | Flat |
| GBP weekly posts | **Yes (Publer auto)** | NEW — 12-event backlog cleared 4/21 |
| Instagram | ~13K followers | Flat |
| Facebook | 864 followers, 5 reviews | Flat |
| TikTok | 1,408 followers | Flat |
| LinkedIn | 106 followers | Flat |
| TripAdvisor | 0 reviews, NOT claimed | Flat |
| Hipcamp | Listed, 0 reviews | Flat |
| Expedia | 8.0 rating | Flat |
| Google ranking (non-brand) | **4 / 10 keywords ranking** (baselined 2026-08-18) | **+4** — first measurement; prior 0/10 carried since April |
| AI engine citations | **4 / 10 cited** (one engine — harness synthesis) | **+4** — ChatGPT/Perplexity/AI Overview still unmeasured |
| Google indexed pages (old site) | ~10 | Flat |
| **DNS cutover** | **✅ Live (4/30)** | NEW — Vercel is now production origin |
| Schema coverage (new site) | **17 / 17 pages** (+`/corporate-retreats/` = 18/18 incl. new landing) | +5 vs. Apr 9 (12/14) |
| Blog individual URLs | **18 posts** | +18 vs. Apr 9 (was 0) |
| Next metrics update | May 7, 2026 | — |
| Improvement tasks done | 3/35 (#4, #6, #10) | Flat |

---

## Property & Deal Summary

- **3 tracts** in same LLC, free and clear (\~$4.2M invested)
- Tract I (9.9 ac) — improved, all operations
- Tract II (11 ac) + III (10.7 ac) — vacant, landlocked
- **Revenue:** 2025 = $350K, 2026 target $500–650K, **Q1 2026 = $61K (on pace)**. Ashley's "safe" mental model is $1M/year.
- Private events 46%, rooms 31%, POS/bar 22%
- **Target event profile:** private party 100+ people, open bar, overnight stays (most profitable configuration per Ashley 2026-04-23)
- **Alcohol:** mandatory through venue (open bar per-person per-hour) as of 2026. Never BYOB. Ordering is a full-time operation (25 distributors).
- **Buyout:** Paul & Donna sent a step-away email 2026-04-23; Ben & Robert countered demanding books/financials; Donna silent but acting unilaterally. Blocked on Exhibit A. Ashley floated a lowball option so remaining members own outright.
- **Christopher:** potential incoming partner, Adam running point. Per Ashley, business plan with realistic projections is the next move to close him.

## People

WhoRoleNancyCo-owner. Deal decision-maker. Needs the Paul/Donna-exit summary.AshleyRuns everything — GBP access, operations. Stretched thin. Target for inquiry-responder voice training.MonetStaff ($28/hr). Target co-owner of shared inquiry inbox.ArlenMaintenance ($170/day)KyliePart-time social ($18/hr)Britney JoGood fit for tours/inquiries per Ashley; has not committedBethBookkeeper (hired via Brian/Paul). **Donna loyalist — do not use as a channel for books/financials.** Even Beth thinks Donna is unreasonable.Paul & Donna (Herchman)Sent step-away email 2026-04-23. Donna still acting unilaterally.Ben & RobertCountered Paul/Donna exit with demand for books. Willing to add capital if needed.ChristopherAdam's potential equity partner. Not yet in; needs business-plan pitch.

## Key Files

- `api/complete.js` — Mark Done serverless function (Vercel, Node 20.x)
- `rancho-done-log.md` — append-only log, source of truth for done state
- `site/improvement-plan.html` — 35 task cards + Plan/Metrics/Audits/Intel/Done tabs
- `brand/2026-04-09-metrics-baseline.md` — live-verified platform data
- `tasks/seo-aeo/` — autonomous SEO/AEO agent workspace
- `site/` — Vercel site (new build, not yet live on main domain)
