# rancho-site-daily — 2026-09-07

## Scope

Daily SEO/AEO run. GOALS.md confirms `rancho-site-daily` is unpaused (resumed 2026-07-15) and not on the Pause List — proceeded.

## Repo state check

- `git status`: 5 files modified-uncommitted — `api/inquiry.js`, `site/css/styles.css`, `site/js/main.js`, `site/pages/contact.html`, `site/pages/weddings.html`. Exact NEEDS OWNER set flagged 2026-08-31, still unchanged, still not this task's — left alone.
- `git log -1` (HEAD) == `git log -1 origin/main` at session start: `a07114b`. Nothing pending from a prior run needed landing today.

## Site health

- `sitemap.xml` → 200. `robots.txt` → 200.
- Spot-checked 7 routes (`/`, `/weddings/`, `/accommodations/`, `/blog/safari-tents-near-austin/` [correctly 404s — root form is canonical], `/safari-tents-near-austin/`, `/corporate-retreats/`, `/videos/`) — all 200 except the deliberate 404, as expected.

## On-page audit — meta description length (new check, not previously run)

Prior runs (8/18, 8/19, 8/31, 9/4, 9/5) measured and disconfirmed internal-link depth, word count + schema depth, exact-match title/H1, and whole-site schema coverage as ranking levers, and CONTEXT.md explicitly instructs not to default to on-page passes since they "pass every validator and change nothing." Meta description length is a different category — it affects SERP snippet truncation and CTR, not ranking — and grep across `run-logs/`, `tasks/seo-aeo/`, `CHANGELOG.md`, `TODO.md` turned up no prior pass specifically on this.

Checked `<meta name="description">` length across all `site/pages/*.html` + `site/index.html`:

| File | Length (before) | Verdict |
|---|---|---|
| `blog.html` | 172 | over ~155-160 truncation point |
| `faqs.html` | 171 | over |
| `videos.html` | 167 | over |
| `wedding-venues-near-austin.html` | 180 | over |
| all other pages | ≤165 | clean |

**Fixed.** Read `VOICE-GUIDE.md` first per Rule 8 (`tasks/seo-aeo/agent-rules.md`) — none of the four used banned terms ("luxury", "Hill Country", "Manor", "General Store", unit counts), so trimming was purely a length edit, not a voice rewrite. Trimmed all four to 121-132 chars, preserving meaning:

- `blog.html`: 172 → 130 chars
- `faqs.html`: 171 → 125 chars
- `videos.html`: 167 → 132 chars
- `wedding-venues-near-austin.html`: 180 → 121 chars

`og:description` / `twitter:description` tags on these pages were already shorter and already distinct from the SERP description — left untouched.

`npm run validate:site` → passes. Committed `4f4534f`, pushed to `origin/main` (`a07114b..4f4534f`). **Verified at the destination, not from the working tree**: `git ls-remote origin refs/heads/main` matches local HEAD; polled the live site until the new content appeared, then confirmed all 4 URLs serve the trimmed description (`/blog/`, `/faqs/`, `/videos/`, `/blog/wedding-venues-near-austin/`).

## Re-Verify Gate

Brand canary (`WebSearch "Rancho Moonrise"`) passed first — ranchomoonrise.com home + `/events/` both returned — session not degraded.

| Claim | Live check | Result |
|---|---|---|
| `/safari-tents-near-austin/` not indexed | `site:ranchomoonrise.com safari tents` via harness `WebSearch` | **still_true** — 8 owned URLs returned (`/corporate-retreats/`, `/accommodations/`, `/weddings/`, `/blog/corporate-retreat-near-austin/`, `/host-your-event/`, `/`, `/faqs/`, `/blog/things-to-do-manor-tx/`), never the target page. On-site diagnosis stays closed per the 8/31 finding — no further on-site work queued against this URL. |
| 5-file NEEDS OWNER uncommitted set | `git status` | **still_true** — identical file list to 8/31, untouched, not this task's to land or discard. |

Gate: 2 claims checked, 2 still_true, 0 resolved, 0 new.

**Side observation, not a gate claim (already an open NEEDS ADAM item, not new):** the brand-canary query's AI-generated answer still echoes *"The property contains 20 luxury cabins and safari tents for up to 50 guests"* — the same banned syndicated Knot/Hotels.com line flagged 2026-08-19. Confirms that item is still live and still being read back as Rancho's own description by at least one answer surface. No new action — fix remains off-domain, with Adam.

## Off-page / next real levers (unchanged, not actioned)

- **Glamping Hub submission** — still the single highest-leverage open item (free, ~15 min, now ~21 weeks untouched). Permission-gated third-party form submission, not a Claude Code technical task — belongs with Adam/Ashley.
- **Hipcamp curation gap** — unchanged, still needs Ashley's call.
- **s7 SERP baseline re-run** — due ~2026-09-18, not yet.
- **Second-engine AEO measurement** — still blocked on a paid API key, dead-ended 2026-09-04.

## Improvement Plan task mapping

No task ID (`rancho-seo-s1`–`s8` etc.) maps cleanly to a meta-description-length fix — it's a general SEO hygiene item, not one of the tracked backlog cards. No done-log entry written.

## Files touched

`site/pages/blog.html`, `site/pages/faqs.html`, `site/pages/videos.html`, `site/pages/wedding-venues-near-austin.html` (meta description trims, committed `4f4534f`), `CONTEXT.md`, `CHANGELOG.md`, `TODO.md`, `run-logs/2026-09-07-seo.md` (documentation).
