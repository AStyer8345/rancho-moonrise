# rancho-site-daily — 2026-07-23

**Status:** shipped edit (2nd consecutive shipping run; cruise-control era ended 2026-07-22)
**Pause gate:** clear — GOALS.md "Keep running" lists `rancho-site-daily`; site/content work RESUMED 2026-07-15 (outreach-only pause).

## Shipped

Filled the #1 pre-scoped autonomous slot from CONTEXT.md "What's Next": `videos.html` schema + sitemap discoverability.

- **BreadcrumbList** JSON-LD added to `site/pages/videos.html` (Home → Videos). Was missing; every other content page carries it.
- **ItemList of 3 VideoObject** added — the embeddable YouTube videos on the page, each with a **real** `uploadDate` + working `hqdefault` thumbnail, verified live before writing:
  - `rHz9c8DbgQA` "Rancho Moonrise in Manor, TX — #RealNotRender" — uploadDate 2022-07-08 (from watch-page `uploadDate` meta)
  - `l-tOXqwDHAI` "Save This for Your Next Texas Getaway" — uploadDate 2026-03-29
  - `IouPSeoTC5I` "Rancho Moonrise" — uploadDate 2026-03-29
  - `name` fields mirror the visible card titles verbatim; `description` authored voice-compliant ("20 minutes from downtown Austin", no banned terms).
- **`/videos/` added to `site/sitemap.xml`** — was absent from the sitemap entirely (live + repo), despite being footer-linked. priority 0.6, changefreq monthly, lastmod 2026-07-23.

2 files, +72 lines, additive-only. Commit `089439f`, pushed to `main`.

## Verified

- `npm run validate:site` → passes
- 3/3 JSON-LD blocks on videos.html parse (CollectionPage, BreadcrumbList, ItemList)
- 3/3 VideoObject required fields (name, description, thumbnailUrl, uploadDate, contentUrl) asserted present
- 3/3 thumbnail URLs return 200; sitemap XML well-formed
- Live deploy confirmed post-push (VideoObject + /videos/ sitemap entry serving from apex)

## Re-Verify Gate (live)

| Claim | Result |
|---|---|
| CONTEXT.md: "videos.html carries no JSON-LD at all" | **PARTIAL/STALE** — live page had a `CollectionPage` block. Corrected. Real gap (BreadcrumbList + VideoObject/ItemList) confirmed absent live and now shipped. |
| apex 200 + server Vercel + cache HIT | still_true |
| /videos/ 200 | still_true |
| /videos/ in sitemap | **was false** (0 hits live+repo) → resolved this run |

## Flagged (not fixed this run — content change out of schema scope)

- **Removed YouTube video still on the page:** the 4th YouTube card `_klefu2vTwM` ("Escape to Rancho Moonrise") is a **removed/private video** — oembed returns 403, `hqdefault.jpg` 404. Its card renders a broken thumbnail image on the live page. Excluded from VideoObject schema deliberately. → logged to TODO.md NEEDS ADAM + FLAG_FOR_ADAM below.

## FLAG_FOR_ADAM

- Videos page card "Escape to Rancho Moonrise" points at a removed YouTube video (`_klefu2vTwM`) — broken thumbnail live. Either re-upload the video to YouTube or remove the card. 30-second fix once decided; I left the card in place rather than silently deleting visible content.

## Ownership notes

- Indexing/SERP → competitive-weekly + Adam (GSC). `/corporate-retreats/` + `/safari-tents-near-austin/` crawl lag unchanged, not re-litigated here.
- Review/GBP → review-monitor / gbp-weekly. Not touched.

## Next slots (remaining surgical runway)

1. `accessibility.html` (no schema) + `policies.html` (BreadcrumbList only) lack `WebPage`/`speakable` — small, low-risk.
2. Internal-linking deepening (cluster floor 4 → 5 inbound topical links) — pre-requires an audit pass.
3. AEO baseline (`rancho-seo-s7`) — still gated on GSC crawl of the two lagging pages.
