# rancho-site-daily — 2026-09-05

## Scope

Daily SEO/AEO cruise-control run. GOALS.md confirms `rancho-site-daily` is unpaused (resumed 2026-07-15) and not on the Pause List — proceeded.

## Repo state check

- `git status`: 5 files modified-uncommitted — `api/inquiry.js`, `site/css/styles.css`, `site/js/main.js`, `site/pages/contact.html`, `site/pages/weddings.html`. This is the exact NEEDS OWNER set flagged 2026-08-31 (contact-form budget field, Flodesk embed, blog CSS, and the 44-line `weddings.html` accommodations-section deletion). **Unchanged, still not this task's — left alone.**
- `git log -1` (HEAD) == `git log -1 origin/main`: `d577a6d7`. Nothing pending from a prior run needs landing today — unlike 8/25→8/31, there is no "shipped but uncommitted" gap this time.

## Site health

- `server: Vercel`, `x-vercel-cache: HIT` on `/`.
- `sitemap.xml` → 200 (ttfb 0.52s). `robots.txt` → 200.
- All **30** URLs listed in `sitemap.xml` checked individually via `curl -o /dev/null -w %{http_code}` — **30/30 return 200**. No broken sitemap entries.
- 6 spot-checked routes (`/`, `weddings.html`, `accommodations.html`, `contact.html`, `/corporate-retreats/`, `/safari-tents-near-austin/`) all 200, TTFB 0.23–0.60s.
- PageSpeed Insights API: `curl` returned **"Quota exceeded for quota metric 'Queries'"** — same missing-API-key family as the standing SERP/Places API asks (`memory` / `TODO.md`). Not logged as a new finding; existing "nice to have API key" framing already covers it. TTFB-based check above stands in as today's speed signal — no regression evident.

## Re-Verify Gate

Brand canary (`WebSearch "Rancho Moonrise"`) passed first — ranchomoonrise.com home + `/events/` both returned — session not degraded.

| Claim | Live check | Result |
|---|---|---|
| `/safari-tents-near-austin/` not indexed | `site:ranchomoonrise.com safari tents near austin` via harness `WebSearch` | **still_true** — 10 owned URLs returned (`/accommodations/`, `/corporate-retreats/`, `/`, `/host-your-event/`, `/faqs/`, 5 blog posts), never the target page. Page itself still live (200) at its root URL. Uncrawled duration: **~126d as of 2026-08-31 + 5 days = ~131d**. On-site diagnosis stays closed per 8/31's exhaustive finding — no further on-site work queued against this URL, per standing instruction. Only lever remains Adam's URL Inspection → Request Indexing. |
| 5-file NEEDS OWNER uncommitted set | `git status` | **still_true** — identical file list to 8/31, untouched, not this task's to land or discard. |
| sameAs entity graph (15 URLs incl. WeddingWire) | not re-checked today — added yesterday (2026-09-04), one day old, doesn't meet the "appeared 2+ runs" re-verify trigger yet | not re-verified (too fresh) |

Gate: 2 claims checked, 2 still_true, 0 resolved, 0 new.

## On-page work

None shipped. Per the standing 2026-09-04/08-31 finding — internal-link depth, word count + schema depth, exact-match title/H1, and whole-site schema coverage are all measured and disconfirmed as ranking levers on this property, and CONTEXT.md explicitly instructs *"do not propose on-page passes as default autonomous work — they pass every validator and change nothing."* Nothing in today's health/gate check surfaced a genuine defect (unlike 8/25's banned-copy template or 8/31's instruction-layer staleness), so there is nothing concrete to fix today without manufacturing busywork. Checked for a defect before concluding there was none — did not stop at "nothing came to mind."

## Off-page / next real levers (unchanged, not actioned)

- **Glamping Hub submission** — still the single highest-leverage open item (free, ~15 min, now ~20 weeks untouched). Not attempted: submitting a third-party listing form with business details is a permission-gated action (form submission on Adam's behalf) this task cannot clear autonomously, and it also isn't a Claude Code technical task — it belongs with Adam/Ashley.
- **Hipcamp curation gap** — unchanged, still needs Ashley's call on whether bookings-via-Hipcamp is even wanted.
- **s7 SERP baseline re-run** — due ~2026-09-18, not yet.
- **Second-engine AEO measurement** — still blocked on a paid API key (ChatGPT/Perplexity/Bing), confirmed dead-ended 2026-09-04.

## Improvement Plan task mapping

No task ID progressed today — nothing shipped against `rancho-seo-s1`–`s8` or the other tracked IDs. No done-log entry written (correctly, per "do not log 0%").

## Files touched

`CONTEXT.md`, `CHANGELOG.md`, `TODO.md`, `run-logs/2026-09-05-seo.md` — documentation only, no site code changed.
