# 2026-05-17 — rancho-site-daily

## Task

**`wordCount` enrichment on BlogPosting JSON-LD across the 17-post blog cluster.** Pre-scoped by 5/16 run-log's "next slot" section as the cleanest next autonomous run. Surgical schema-only edits — no body changes.

## Re-Verify Gate

| Claim | Check | Result |
|---|---|---|
| apex serves Vercel | `curl -I https://ranchomoonrise.com/` → `server: Vercel`, `x-vercel-cache: HIT` | still_true |
| www 308 → apex | `curl -I https://www.ranchomoonrise.com/` → 308, location apex | still_true |
| sitemap 200 | `curl -o /dev/null -w '%{http_code}'` | 200, still_true |
| /corporate-retreats/ 200 | same | 200, still_true |
| /safari-tents-near-austin/ 200 | same | 200, still_true |
| /blog/* — 17 BlogPosting pages all 200 | sampled via post-edit `npm run validate:site` | still_true |

**Resolved this run: 0.** No stale claims required auto-resolution.

## What I did

Added `wordCount` to the `BlogPosting` JSON-LD on each of the 17 blog cluster posts. `wordCount` is a schema.org Article property supported by all major engines and useful for AEO "how long is this article" intent — particularly relevant for AI-engine result framing ("a 1,200-word guide to…"). Until today, every BlogPosting block on the site lacked it.

### Method

1. Enumerated all pages containing `"@type": "BlogPosting"` JSON-LD — 17 files.
2. For each page, extracted the article body via a single regex pass: preferred `<article>…</article>` if present (10 pages), else `<main>…</main>` (7 pages). Stripped `<script>`, `<style>`, `<nav>`, `<header>`, `<footer>`, `<aside>` first to avoid counting nav/footer chrome. Counted tokens matching `[A-Za-z0-9][A-Za-z0-9'’\-]*` — punctuation-tolerant, contraction-friendly.
3. Inserted `"wordCount": NNNN,` between `"dateModified"` and `"url"` in each BlogPosting JSON-LD block.
4. Bumped `dateModified` to `2026-05-17` (schema actually changed today).
5. Bumped corresponding sitemap.xml `<lastmod>` to `2026-05-17` for each of the 17 blog URLs.

### Word counts (article body, distilled)

| Post | wordCount | Wrapper |
|---|---:|---|
| austin-bachelorette-ranch-vs-bar-crawl | 1,284 | article |
| bachelorette-party-austin-texas | 1,521 | main |
| birthday-party-venue-near-austin | 1,358 | article |
| corporate-retreat-near-austin | 1,106 | main |
| corporate-retreat-planning-guide-texas | 1,993 | main |
| corporate-retreat-ranch-vs-hotel | 1,209 | article |
| glamping-near-austin-texas | 1,005 | main |
| glamping-vs-camping | 1,149 | article |
| mothers-day-near-austin | 1,031 | article |
| pool-day-pass-austin | 1,115 | article |
| ranch-wedding-texas | 1,546 | main |
| summer-glamping-near-austin | 1,329 | article |
| things-to-do-manor-tx | 1,235 | main |
| things-to-do-near-austin-with-kids | 1,487 | article |
| wedding-venues-near-austin | 1,256 | main |
| weekend-getaways-near-austin | 1,159 | article |
| yoga-retreat-near-austin | 1,300 | article |

Total cluster wordCount: 22,083 words. Range 1,005–1,993. Median ~1,256. No post dips below the 1,000-word "thin content" risk floor that Google has historically flagged in core-update post-mortems.

### Voice / data compliance

- No body content changed. `wordCount` is computed from existing body text. No new copy was written.
- VOICE-GUIDE compliance is unchanged from prior state.
- Accuracy note: 7 pages without an `<article>` wrapper count FAQ accordion + Related Reading list contents because those are inside `<main>`. This is the consistent extraction rule — and FAQ/Related-Reading copy IS substantive answer content that engines parse, so including it is defensible. No post was over-counted by more than ~50–100 words from this — the dominant body sections are still the dominant counted content.

### Why this run over `/corporate-retreats/` nav promotion or Person-author schema

`wordCount` enrichment is (a) autonomous (no NEEDS ADAM), (b) cluster-wide rather than single-page (17 posts in one slot), (c) cheap to validate (JSON parse), (d) repeatable as the cluster grows. Nav promotion needs Adam's confirm; Person-author schema is BLOCKED on the author-byline decision. Today's slot is the unambiguous next-best autonomous step.

## Validation

- `npm run validate:site` — passes.
- Parsed every BlogPosting JSON-LD block via `python3 json.loads`; all 17 are valid JSON; all 17 have `"wordCount": <int>` and `"dateModified": "2026-05-17"`.
- Live Re-Verify Gate: apex 200 + `server: Vercel` + `x-vercel-cache: HIT`; www 308 → apex; sitemap 200.

## Diff

18 files (17 blog pages + sitemap.xml), 51 insertions, 34 deletions. Sitemap deletions are date replacements. Per-file changes are 2-line insertions (`dateModified` swap + new `wordCount` line). Pre-existing uncommitted prior-session changes in `styles.css`, `main.js`, `weddings.html`, `contact.html`, `api/inquiry.js` intentionally NOT staged (5/7–5/16 convention).

## Improvement-plan task mapping

This run does NOT cleanly map to any of the 8 named SEO task IDs (s1–s8). `wordCount` is post-level schema enrichment, distinct from:
- s3 (topical clusters — internal linking, not schema)
- s4 (Breadcrumb + Speakable schema — different schema types)
- s7 (AEO baseline measurement — output measurement, not schema)

No done-log entry. (s4 was held at PROGRESS 90% by 5/15 run; today's `wordCount` enrichment could be argued to nudge that — but `wordCount` is on `Article`/`BlogPosting`, not `Breadcrumb`/`Speakable`, so I'm not claiming progress against s4.)

## Pre-scoping for next slot

In priority order:

1. **Hipcamp strategy follow-through.** Two voice/data fixes that are autonomous regardless of Ashley's `isPrivate` answer: acreage drift ("34-acre" in listing overview vs. VOICE-GUIDE 36) and the bar-walk-in voice violation ("unwind with a drink at our bar"). Both ~5 min if I can edit the Hipcamp listing — but Hipcamp dashboard requires Ashley's login. **Re-classify**: this is NEEDS ADAM, not autonomous. Skip for autonomous slot.

2. **AEO baseline measurement (rancho-seo-s7).** Still deferred until apex is in Google's index. GSC submit remains the gate.

3. **Promote `/corporate-retreats/` into main nav.** Currently footer only. Needs Adam confirm.

4. **Author Person schema for BlogPosting JSON-LD.** BLOCKED on NEEDS ADAM author-byline decision.

5. **`articleSection` enrichment on BlogPosting JSON-LD.** Surgical, autonomous, schema.org-recognized, useful for AI-engine topic classification — would categorize each post under "Weddings" / "Corporate Retreats" / "Glamping" / "Things to Do" / "Bachelorette" / "Events". Cluster-wide. Candidate next slot.

6. **Image schema enrichment.** Several BlogPosting blocks have `"image": "<url-string>"`. Schema.org prefers `ImageObject` with `width`/`height`/`caption`. Autonomous if dimensions are available from the file system (they are — WebP responsive ladder).

## Outcome

17 BlogPosting JSON-LD blocks enriched with `wordCount`. Total cluster word coverage: 22,083 words. All blocks parse-valid. Sitemap synced. Validation passes. Re-Verify Gate: 6/6 still_true, 0 resolved.
