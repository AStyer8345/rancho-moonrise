# 2026-05-02 — rancho-site-daily

**Goal:** internal-linking pass to give yesterday's new `/corporate-retreats/` landing page inbound link equity from day 1.

## Re-Verify Gate

| Claim | Live result | Verdict |
|---|---|---|
| DNS on Vercel | `curl -I https://ranchomoonrise.com/` → 200 + `server: Vercel` + `x-vercel-cache: HIT` | still_true |
| `www` host redirects to apex | `curl -I https://www.ranchomoonrise.com/` → 308 + `server: Vercel` | still_true |
| `/sitemap.xml` returns 200 | `curl -o /dev/null -w '%{http_code}'` → 200 | still_true |
| `/corporate-retreats/` returns 200 | `curl -o /dev/null -w '%{http_code}'` → 200 | still_true |

```
[2026-05-02 08:00] re-verify dns-on-vercel — still_true — live=server:Vercel prior=server:Vercel
[2026-05-02 08:00] re-verify www-redirect-to-apex — still_true — live=308 prior=308
[2026-05-02 08:00] re-verify sitemap-200 — still_true — live=200 prior=200
[2026-05-02 08:00] re-verify corporate-retreats-200 — still_true — live=200 prior=200
```

No prior claims required auto-resolution.

## What I did

Pre-pass count via `grep -c '"/corporate-retreats/"'` on all candidate sources: **0** internal inbound links from any source page. Post-pass: **7** inbound links across **6** source pages.

| File | Edit | Inbound links to `/corporate-retreats/` |
|---|---|---|
| `site/index.html` | Inline `<a>` on "corporate retreat" in About Split paragraph | 1 |
| `site/pages/host-your-event.html` | Corporate Retreats feature card href: `/blog/corporate-retreat-near-austin/` → `/corporate-retreats/` | 1 |
| `site/pages/weddings.html` | Added "Corporate Retreats at Rancho Moonrise" to Related Reading list | 1 |
| `site/pages/corporate-retreat-near-austin.html` | Added top-of-list Related Reading + replaced duplicate `/host-your-event/` feature card with canonical venue page; also added `/blog/corporate-retreat-ranch-vs-hotel/` cross-link | 2 |
| `site/pages/corporate-retreat-planning-guide-texas.html` | Top-of-list Related Reading + ranch-vs-hotel cross-link | 1 |
| `site/pages/corporate-retreat-ranch-vs-hotel.html` | Top-of-list "More on Corporate Retreats" | 1 |

**Constraint hit:** the homepage Event Barn duo card was a candidate but its parent element is already an `<a href="/host-your-event/">`. Nested anchors are invalid HTML, so the natural homepage anchor is the About Split paragraph instead.

**Sitemap freshness**: 7 lastmod entries bumped to 2026-05-02 — `/`, `/weddings/`, `/host-your-event/`, `/corporate-retreats/`, `/blog/corporate-retreat-near-austin/`, `/blog/corporate-retreat-planning-guide-texas/`, `/blog/corporate-retreat-ranch-vs-hotel/`.

## Validation

- `npm run validate:site` → "Site validation passed." (JSON-LD parse, local asset references, sitemap rewrite coverage, apex canonical, stale-claim patterns, clean-route hygiene all green)
- Final inbound link audit via grep matches the table above (7 total).

## Improvement-plan progress

- `rancho-seo-s3-topical-clusters` — PROGRESS 95% (was 90%). Internal-linking is the core mechanism for topical cluster authority; this pass adds the new corporate-retreats venue page to the hub-and-spoke, with the host-your-event feature card now pointing to the canonical venue rather than a blog post.

## FLAG_FOR_ADAM

- None new. Existing flags unchanged: GSC sitemap submit, GA4/GTM ID, Calendly virtual URL, low-res JPG re-uploads. (See CONTEXT.md "What's Next" for the live list.)

## Next-run priorities

1. **rancho-seo-s7 AEO baseline** — query AI engines for the 10 target keywords now that the corporate-retreat cluster is fully wired.
2. Promote `/corporate-retreats/` into the main nav (currently footer only) once Adam confirms.
3. Continue topical-cluster strengthening on weaker hubs (e.g., bachelorette / pool day pass clusters).
