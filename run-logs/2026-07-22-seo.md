# rancho-site-daily — 2026-07-22

**Cruise-control streak BROKEN at 39. First shipped edit since 2026-05-26 (57 days).**
Shipped: `WebPage`/`SpeakableSpecification` + `FAQPage` schema on `/blog/weekend-getaways-near-austin/` — the last blog-cluster AEO gap.

## Gate check — pause LIFTED

The 39-run cruise-control gate was keyed to GOALS.md week-of-5/18 reading "No Rancho Moonrise active work — paused." **That is no longer what GOALS.md says.** Live read this run (`/Users/adamstyer/Documents/GOALS.md` → symlink into DOS, mtime **2026-07-20 11:19**):

> "No Rancho Moonrise *outreach* (Christopher Gill) — still paused. **Site/content work RESUMED 2026-07-15.**"
> Paused Workstreams: "Rancho Moonrise outreach (Christopher Gill) — paused. NOTE: DNS cutover resolved 2026-04-30; **Rancho site/content work resumed 2026-07-15**."

The pause was narrowed to *outreach only*. Site/content work has been unblocked since 7/15; the 7/17 and 7/18 runs carried the stale "still paused" read forward and idled through it. Corrected here. Autonomous site/schema work is in-scope again.

**Gap in coverage:** no `rancho-site-daily` run-log exists for 7/19, 7/20, or 7/21 — the task did not fire (or fired without logging) for three days. Not reconstructable; noted, not backfilled.

## Re-Verify Gate (live, 7/7 still_true, 0 resolved)

```
[2026-07-22 08:0x] re-verify apex-200          — still_true — live=200 server:Vercel x-vercel-cache:HIT age:78168 (~21.7hr)
[2026-07-22 08:0x] re-verify www-redirect      — still_true — live=308 → https://ranchomoonrise.com/
[2026-07-22 08:0x] re-verify sitemap-200       — still_true — live=200
[2026-07-22 08:0x] re-verify corp-retreats-200 — still_true — live=200
[2026-07-22 08:0x] re-verify safari-tents-200  — still_true — live=200
[2026-07-22 08:0x] re-verify blog-200          — still_true — live=200
[2026-07-22 08:0x] re-verify robots-200        — still_true — live=200
```

Apex `last-modified: Tue, 21 Jul 2026 17:16:47 GMT` — deploy pipeline alive.

## Schema audit (new this run — the finding)

Built a per-page JSON-LD coverage matrix across all 30 public pages (`index.html` + 29 `pages/*.html`, conflict-copies excluded). Result:

| Type | Coverage before |
|---|---|
| BreadcrumbList | 27/30 — missing: `accessibility.html`, `videos.html`, `index.html` |
| SpeakableSpecification | 25/30 — missing: `accessibility.html`, `blog.html`, `policies.html`, `videos.html`, **`weekend-getaways-near-austin.html`** |

`weekend-getaways-near-austin.html` was the outlier: the **only** page in the 17-post blog cluster carrying `BlogPosting` + `BreadcrumbList` but **no** `WebPage`/`speakable` and **no** `FAQPage` — despite already rendering a complete visible `.aeo-block` Quick-Answers section with 4 Q&A pairs. Real content, zero machine-readable exposure. Cleanest available slot: pure schema-on-existing-content, no copy written, no NEEDS ADAM generated.

`index.html` missing BreadcrumbList is correct-by-design (homepage is the breadcrumb root). `accessibility.html` / `policies.html` / `videos.html` are genuine gaps but lower value — logged to TODO backlog, not shipped here.

## Shipped

**`site/pages/weekend-getaways-near-austin.html`** (+56 lines, schema-only — zero body/copy changes):
1. `WebPage` + `SpeakableSpecification` block — `cssSelector: [".aeo-block h2", ".aeo-block p", ".blog-lead", "h2"]`, byte-identical selector array to the other 16 cluster pages. Verified `.aeo-block` and `.blog-lead` both actually render on this page before writing the selector.
2. `FAQPage` block — 4 Q&As, **extracted programmatically from the rendered `.aeo-block` `<h3>/<p>` pairs**, not authored. Answers are verbatim.
3. `BlogPosting.dateModified` 2026-05-26 → 2026-07-22.

**`site/sitemap.xml`** (+1/-1): `lastmod` for that one URL 2026-05-26 → 2026-07-22. No other URL touched.

Block order/indentation/HTML-comment style mirror `glamping-vs-camping.html` exactly.

### Why FAQ text was extracted, never written

Google treats FAQPage markup whose answers don't appear on-page as a policy violation — manual-action territory, not merely a lost rich result. So the edit script parses the visible DOM, builds the JSON from it, then asserts round-trip equality. No answer text originated in this session.

## Verification

```
Site validation passed.                      (npm run validate:site)
5/5 JSON-LD blocks parse clean on target     (BlogPosting, ItemList, BreadcrumbList, WebPage, FAQPage)
4/4 FAQ Q&A match visible on-page text verbatim   (assertion, not eyeball)
0 invalid JSON-LD blocks across all 30 public pages
blog cluster: 17 pages → 17/17 Speakable, 17/17 FAQPage   (was 16/17, 16/17)
diff: 2 files, 58 insertions, 2 deletions
```

## Continuity

- Pre-existing prior-session changes (`api/inquiry.js`, `styles.css`, `main.js`, `contact.html`, `weddings.html`) intentionally NOT staged — 5/7+ convention holds.
- Concurrent-writer artifacts (`* 2`/`* 3`/`* 4` copies, `AGENTS*.md`, `TODO.md.tmp.*`, duplicated images, `rancho-moonrise-assets/`, `youtube-uploads/`) intentionally NOT staged. Committed by explicit path only.
- HEAD at start was a run of concurrent `notebooklm-sync` commits (`770887c`, `17be5e9`, `ed2a420`) — several reporting expired auth. Not this task's scope; flagged in TODO.

## Ownership boundaries respected

- Indexing/SERP claims (`/corporate-retreats/`, `/safari-tents-near-austin/` uncrawled) owned by `rancho-competitive-weekly` + Adam — not re-litigated.
- Review/GBP claims owned by `rancho-review-monitor` / `rancho-gbp-weekly` — not re-litigated.
- No `AggregateRating` added anywhere. Per `memory/reference_aggregate_rating_self_serving.md`, self-serving review schema earns no SERP stars; the previously pre-scoped "AggregateRating onto BlogPosting" candidate is **retired**, not deferred.

## Improvement-plan mapping

`rancho-seo-s4-schema-breadcrumb-speakable` — PROGRESS 90%. Blog cluster now complete (17/17 both types); 3 non-cluster pages (`accessibility`, `policies`, `videos`) still lack Speakable, and `videos.html` carries no JSON-LD at all.
