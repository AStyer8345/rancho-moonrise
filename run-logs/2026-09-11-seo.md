# rancho-site-daily — 2026-09-11

## Repo state at start

- `git status`: modified (uncommitted) — exactly the standing 5-file NEEDS OWNER set (`api/inquiry.js`, `site/css/styles.css`, `site/js/main.js`, `site/pages/contact.html`, `site/pages/weddings.html`). Confirmed byte-identical to the 8/31 baseline via `git diff --stat` (same byte/line counts: `api/inquiry.js` Bin 6182→6214, `site/css/styles.css` +114, `site/js/main.js` +58, `site/pages/contact.html` +11, `site/pages/weddings.html` -44).
- `git log` HEAD was **2 commits ahead of `origin/main`**: `e7fd346` (notebooklm sync log, +3 lines to `run-logs/notebooklm-sync.md`) and `e2da4f9` (notebooklm push-failure log, +1 line, same file). Both benign — inspected via `git show --stat` before pushing. Pushed at run start: `git push origin main` → `ce24cd7..e2da4f9 main -> main`. Re-verified at the destination: `git fetch origin main` + `git rev-parse HEAD origin/main` both return `e2da4f9`.

This is this run's actual shipped action — a small repo-hygiene fix (landing 2 stranded local commits), not an on-page edit.

## Re-Verify Gate

1. **Claim: `/safari-tents-near-austin/` absent from Google's index (on-site diagnosis closed 8/31, do not queue further on-site work).**
   - Brand canary first: `site:ranchomoonrise.com Rancho Moonrise` returned 10 owned URLs (index, /weddings/, /accommodations/, /blog/, /faqs/, /events/, /host-your-event/, /corporate-retreats/, /room-item/cabins/, /videos/) — session not degraded.
   - `site:ranchomoonrise.com safari tents` → same 8-owned-URL set as 8/31–9/10 (`/accommodations/`, `/`, `/blog/corporate-retreat-near-austin/`, `/host-your-event/`, `/corporate-retreats/`, `/weddings/`, `/faqs/`, `/blog/things-to-do-manor-tx/`). Page itself still never appears. By the established day-count (126 on 8/31, 131 on 9/5, both matching straight-calendar-day elapsed), 9/11 is ~137 days.
   - Result: **still_true**. Per the standing 8/31 conclusion, no further on-site work queued.

2. **Claim: 5-file NEEDS OWNER uncommitted set unchanged.**
   - `git diff --stat` on the 5 known files matches the 8/31 byte/line counts exactly (see above). No 6th file present.
   - Result: **still_true**.

`[2026-09-11 08:00] re-verify safari-tents-indexing — still_true — live=absent(8 owned URLs) prior=absent(8 owned URLs)`
`[2026-09-11 08:00] re-verify 5-file-needs-owner-set — still_true — live=5 files (byte-identical) prior=5 files`

## Site health

- `sitemap.xml` 200, `robots.txt` 200.
- All 30 sitemap URLs individually curled: **30/30 200**.
- `npm run validate:site` passes (0 invalid JSON-LD, VOICE-GUIDE checks clean, all internal assets resolve, sitemap↔vercel.json rewrites consistent).
- TTFB spot check, 6 routes: `/` 0.428s, `/weddings/` 0.280s, `/blog/` 0.326s, `/accommodations/` 0.276s, `/contact/` 0.246s, `/safari-tents-near-austin/` 0.222s — all in the normal 0.22-0.50s range this property has read all month, no regression.
- PageSpeed Insights API: still `429 rateLimitExceeded` (quota exhausted) — same known blocker family flagged 9/5, not a new finding, not re-flagged separately.

## On-page levers checked (new this run — not previously logged in run history)

Four on-page levers (internal-link depth, word count + schema depth, exact-match title/H1, meta-description length) are already measured and either disconfirmed as ranking factors or swept clean. Rather than re-check an already-dry lever, checked two levers with no prior entry in this task's run history:

1. **Alt-text coverage.** Parsed every `<img>` tag across all 31 public HTML pages (same file set `validate-site.py` walks, minus admin/audit pages): **251 total `<img>` tags, 251 carry an `alt` attribute (0 missing entirely)**. 2 carry `alt=""` — both are the `.lightbox__img` placeholder in `weddings.html` and its untracked duplicate `weddings 2.html`, a JS-templated element whose `alt` is set at runtime from the clicked thumbnail's own alt text, not a static content image. Clean.
2. **H1 uniqueness.** Counted `<h1>` occurrences per page across the same 31-file set (excluding the untracked `weddings 2.html` duplicate): **31/31 pages carry exactly one `<h1>`**. Clean.

Both levers measured dry — nothing to fix, not skipped.

## Minor hygiene, not actioned

- `site/pages/weddings 2.html` — still present, untracked, byte-identical to `site/pages/weddings.html`, unreferenced anywhere in the codebase. Same file-sync-duplicate family logged 8/31 and 9/10. Zero SEO/functional impact. Left alone.

## Outcome

Real action this run was repo hygiene (2 stranded commits pushed), not an on-page edit — a genuine gap (2 unpushed commits sitting locally since yesterday) rather than a manufactured one. On-page side stayed quiet: two new levers checked, both clean, nothing fabricated to fill the slot.

Gate: 2 claims, 2 still_true.
