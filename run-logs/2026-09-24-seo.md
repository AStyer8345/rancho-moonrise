# rancho-site-daily — 2026-09-24

## Repo state at start
- Main checkout `/Users/adamstyer/Documents/rancho-moonrise` still detached mid-rebase ("HEAD (no branch, rebasing main)", dirty `run-logs/notebooklm-sync.md`). Not touched — same rule as 9/20, 9/21.
- Worked from `git worktree add --detach ../rancho-moonrise-daily-20260924 origin/main` (HEAD `bbc99ad`). Another worktree (`../rancho-moonrise-review-20260923`) exists from a different task; left alone.

## Re-Verify Gate
1. **Brand canary** `Rancho Moonrise glamping events Manor TX` → 5 owned URLs (/contact/, /blog/, /blog/things-to-do-manor-tx/, /events/, /). Session healthy. (Answer text again echoed the banned "luxury cabins and safari tents … up to 50 guests" line — existing off-domain NEEDS ADAM item.)
2. **`/safari-tents-near-austin/` absent from index.** `site:ranchomoonrise.com safari tents` → 9 owned URLs, same set as 9/21; target absent. Inbound links re-checked: linked from accommodations, blog index, and 3 blog posts — not orphaned. **still_true** (owner URL Inspection is the only lever).
3. **Sitemap / routes.** `sitemap.xml` 200; 31/31 URLs 200. One URL read 0.69s TTFB on first pass, 0.33s on retest — noise. **still_true.**
4. **Dead YouTube card on `/videos/` (`_klefu2vTwM`).** Thumbnail 404, oembed 403 ("Forbidden"). **still_true, ~day 63** since first flagged 7/23. Schema correctly excludes it.
5. **`/host-your-event/` "1,000+ guests"** vs Barn 200 / schema `maximumAttendeeCapacity: 200`. Live HTML: 2 occurrences. **still_true** — owner answer needed.
6. **Main checkout mid-rebase.** Live `git status -sb` → still rebasing. **still_true.**

`[2026-09-24 09:25] re-verify safari-tents-indexing — still_true — live=absent(9 owned URLs) prior=absent(9 owned URLs)`
`[2026-09-24 09:25] re-verify sitemap-routes-200 — still_true — live=31/31 prior=31/31`
`[2026-09-24 09:25] re-verify videos-dead-embed — still_true — live=thumb 404 / oembed 403 prior=same`
`[2026-09-24 09:25] re-verify host-your-event-1000-guests — still_true — live=2 occurrences prior=2`
`[2026-09-24 09:25] re-verify main-checkout-mid-rebase — still_true — live=rebasing prior=rebasing`

Not this task's (skipped): review count / reply state / GBP cadence.

## Checks
- `npm run validate:site` passes.
- PageSpeed API: quota exceeded (known blocker family). The 9/16 audit's live Lighthouse (99/100/100/100) stays the latest measurement.
- GSC: still not wired to any path this task can read — owner item.
- **Image dimensions (CLS) — first sweep in this task's history.** 28 of 222 `<img>` lack width/height. All are absolutely positioned / object-fit:cover inside CSS-sized containers (heroes, CTA backgrounds, video thumbnails in aspect-ratio embeds) or in unlinked internal pages (mockup-preview, report). No layout-shift risk. Clean.
- **Canonical + robots meta — live sweep, 31 URLs.** Every page has exactly one canonical equal to its own URL; no `noindex`. Clean.
- Titles: 15 pages at 61–68 chars. Already reviewed and left on 9/20 (brand suffix is the truncated tail). Not reopened.

## Shipped
No site change. Every on-page lever checked came back clean, and I did not make one up.
Instead closed 5 stale TODO items, each checked against live state first:
- "Create `rancho-review-monitor` task" — exists, RUN_079 on 9/20.
- "Add Re-Verify Gate to site-daily SKILL" / "…competitive-weekly SKILL" — both SKILL.md files carry the gate.
- "Wedding inquiry form backend" (×2) — `site/js/main.js` intercepts submit and POSTs to `/api/inquiry/` (`api/inquiry.js`). `action="#"` is the no-JS fallback only.

## Improvement plan mapping
Nothing mapped to a plan task id. No done-log line written.

## FLAG_FOR_ADAM (all re-verified today)
- Dead video card on `/videos/` — ~63 days. Re-upload or delete the card (30 seconds once decided).
- `/host-your-event/` says "1,000+ guests" — confirm the real ceiling.
- `/safari-tents-near-austin/` — GSC URL Inspection → Request indexing.
- Main checkout stuck mid-rebase since 9/18 — needs `git rebase --abort` or `--continue` by a human.
