# 2026-05-16 — rancho-site-daily

## Task

**Hipcamp curation-gap audit.** Pure research output, no HTML edits. Pre-scoped by 5/15 run-log's "next slot" section.

## Re-Verify Gate

| Claim | Check | Result |
|---|---|---|
| apex serves Vercel | `curl -I https://ranchomoonrise.com/` → `server: Vercel`, `x-vercel-cache: HIT` | still_true |
| www 308 → apex | `curl -I https://www.ranchomoonrise.com/` → 308, location apex | still_true |
| sitemap 200 | `curl -o /dev/null -w '%{http_code}'` | 200, still_true |
| /corporate-retreats/ 200 | same | 200, still_true |
| /safari-tents-near-austin/ 200 | same | 200, still_true |
| /blog/things-to-do-near-austin-with-kids/ 200 | same | 200, still_true |
| /blog/weekend-getaways-near-austin/ 200 | same | 200, still_true |
| /blog/things-to-do-manor-tx/ 200 | same | 200, still_true |
| /blog/corporate-retreat-planning-guide-texas/ 200 | same | 200, still_true |
| /faqs/ 200 | same | 200, still_true |
| Rancho NOT in Hipcamp Austin editorial set | grep across 1.32 MB live HTML | still_true — 0 hits across all variations |

**Resolved this run: 0.** No stale claims required auto-resolution.

## What I did

Fetched Hipcamp's Austin glamping editorial landing page and Rancho's Hipcamp listing, parsed the embedded `__NEXT_DATA__` payloads, and produced a side-by-side comparison identifying why Rancho is absent.

**Key findings:**

1. **Distance is not the problem.** Rancho is 14.7 mi NE of downtown Austin (haversine-computed); the editorial set has a peer in Webberville at 16.3 mi and Manchaca at 12.3 mi. Rancho slots cleanly at #4–5 in the geographic ordering.

2. **The structural exclusion is `isPrivate: true` + `isReviewable: false` + state-level `discoverUrl`.** All three would have to flip before Rancho is eligible for surfacing. The `discoverUrl` is `/en-US/d/united-states/texas/camping/all` — TX state, not Austin city.

3. **Hipcamp's editorial weight is reviews-driven.** Lowest review count in surfacing set is 12 (The Outpost). Median is ~63. Rancho's listing has **0 reviews** and `recommendsViewable: false` — the review flywheel cannot start by configuration.

4. **Content quality has 8 named gaps** even before structural fixes — including listing overview missing "glamping" and "safari tent" keywords, 24 uploaded photos with empty descriptions, empty `highlights` + `activities` arrays, acreage drift (overview "34", data 37, VOICE-GUIDE 36), and a voice violation ("unwind with a drink at our bar" when VOICE-GUIDE specifies the bar is event-only).

5. **Strategic question raised, not answered.** The `isPrivate: true` flag may be a deliberate Ashley/operator choice to use Hipcamp as SEO presence only and not a real booking channel. The audit explicitly flags this for Adam to confirm with Ashley before any dashboard changes are made.

**Output:** `brand/2026-05-16-hipcamp-curation-gap-audit.md` — 280-line research document with the 20-property editorial table, Rancho's full listing payload, gap analysis ranked by tier, and a dependency-ordered unlock sequence (IF Ashley wants Hipcamp surfacing).

## Why this slot over other autonomous candidates

- 5/15 run-log explicitly named this audit as the #1 next-slot candidate.
- `wordCount` enrichment on BlogPosting JSON-LD (also named) is the cleanest remaining HTML-edit slot — kept queued for next run.
- AEO baseline measurement (rancho-seo-s7) still deferred per ongoing GSC-not-yet-indexed status.

## Improvement-plan mapping

This audit does not map to any of the 8 named SEO/AEO task IDs (s1–s8) or to the broader p2/p3 tasks. Hipcamp surfacing is third-party platform discoverability, not on-site SEO. **No done-log entry.**

## Files touched

- `brand/2026-05-16-hipcamp-curation-gap-audit.md` (new file, ~280 lines)
- `run-logs/2026-05-16-seo.md` (this file)
- `CONTEXT.md` (Last worked on, What's Next, plus a new Hipcamp finding under NEEDS ADAM)
- `CHANGELOG.md` (one dated bullet)
- `TODO.md` (Hipcamp-strategy check added under NEEDS ADAM)

## Files intentionally NOT staged (5/7-5/15 convention)

Prior-session uncommitted changes left untouched:
- `api/inquiry.js`
- `brand/review-aggregate.json`
- `site/admin/dashboard-state.json`
- `site/css/styles.css`
- `site/js/main.js`
- `site/pages/contact.html`
- `site/pages/weddings.html`
- Untracked Dropbox sync duplicates and other untracked files

## Pre-scoping next slot

- **`wordCount` JSON-LD enrichment** across the 18-post BlogPosting cluster — autonomous, surgical, repeatable; useful for AEO "how long is this article" intent. Cleanest next slot.
- **Person schema for BlogPosting authors** — BLOCKED on NEEDS ADAM author-byline decision.
- **Glamping Hub listing copy fix** (~15 min, voice violation) — autonomous if Adam confirms which body of copy is authoritative; otherwise NEEDS ADAM.
- **Article schema audit** on landing pages (currently only BlogPosting on blog cluster) — would extend AggregateRating + author signal to non-blog content.

## Vercel deploy verification

The audit document lives outside `site/` so no Vercel rebuild is triggered. CONTEXT/CHANGELOG/TODO updates trigger a deploy (config files are repo-root). Confirmation deferred to commit step.
