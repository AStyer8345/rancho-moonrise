# rancho-site-daily — 2026-09-26

## Repo state at start
- Main checkout `/Users/adamstyer/Documents/rancho-moonrise` is still detached mid-rebase ("rebasing main", `.git/rebase-merge` dated 2026-09-18). Local-only commits `157efd1` and `824abff` are not on any remote, and `run-logs/notebooklm-sync.md` is dirty. I left all of it alone.
- Worked from `git worktree add --detach ../rancho-moonrise-site-20260926 origin/main` (HEAD `4950521`).
- Production before push: `vercel inspect ranchomoonrise.com` → `dpl_7yVdj39qGG8C9rE79eBMsfpeTTJq`, a git build from 9/25 21:04 CT (review-monitor push). No CLI pin, so a push goes live normally.

## Re-Verify Gate (harness WebSearch + curl)
1. **Brand canary** `Rancho Moonrise glamping events Manor TX` → 5 owned URLs. Session healthy. The answer text still echoes the off-domain "luxury cabins … 50 guests / 34 acres" copy (existing NEEDS ADAM item).
2. `[2026-09-26 08:05] re-verify safari-tents-indexing — still_true — live=absent(9 owned URLs) prior=absent(9 owned URLs)`
3. `[2026-09-26 08:05] re-verify sitemap-routes-200 — still_true — live=32/32 prior=31/31 (+/offthegrid/)`
4. `[2026-09-26 08:05] re-verify videos-dead-embed — still_true — live=2 refs to _klefu2vTwM prior=2`
5. `[2026-09-26 08:05] re-verify host-your-event-1000-guests — still_true — live=2 prior=2`
6. `[2026-09-26 08:05] re-verify main-checkout-rebase — still_true — live=rebasing since 9/18 prior=same`
- Baseline: `site:ranchomoonrise.com "Off the Grid" retreat November` → no `/offthegrid/` URL. Not indexed, day 3 in git. Owner URL Inspection is the lever, and it's time-sensitive (event is Nov 14).
- Not this task's (skipped): review count / reply state / GBP cadence.

## Shipped
- `site/offthegrid/images/hero-pool.webp`: cwebp q85 from the 1536×1024 PNG. Went from 3,012,525 B to 342,044 B (−89%). I checked it by eye and saw no visible artifacts. q80 would have been 280 KB, but I kept q85 because it's a full-bleed hero.
- `site/offthegrid/index.html`: CSS `url('images/hero-pool.png')` → `.webp`. Added `<link rel="preload" as="image" href="images/hero-pool.webp" type="image/webp" fetchpriority="high">` after the canonical. The CSS background isn't discovered until the stylesheet is parsed, so the preload moves the LCP fetch up to HTML parse time.
- `validate:site` passes; `test:runtime` passes.
- PNG kept in the repo until the live page is verified. TODO added to delete it after that.

## Not done
- PageSpeed not re-run (known quota blocker family). This is an asset-size fix; the lab measurement comes after deploy.
- `/events/` expired Event schema + no link to `/offthegrid/`: still open in TODO (need to check the CRM loader first).
