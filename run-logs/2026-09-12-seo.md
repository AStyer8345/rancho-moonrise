# rancho-site-daily — 2026-09-12

## Repo state at start

- `git status`: clean vs. `origin/main` (HEAD == origin/main, no stranded local commits this run). Modified-but-uncommitted: exactly the standing 5-file NEEDS OWNER set (`api/inquiry.js`, `site/css/styles.css`, `site/js/main.js`, `site/pages/contact.html`, `site/pages/weddings.html`).
- Confirmed byte-identical to the 8/31 baseline: `git diff --stat` matches the same byte/line counts recorded every run since (`api/inquiry.js` Bin 6182→6214, `site/css/styles.css` +114, `site/js/main.js` +58, `site/pages/contact.html` +11, `site/pages/weddings.html` -44). `git log -- <each file>` confirms no commit has touched any of the 5 since the baseline.

## Re-Verify Gate

1. **Claim: `/safari-tents-near-austin/` absent from Google's index (on-site diagnosis closed 8/31, do not queue further on-site work).**
   - Brand canary first: `Rancho Moonrise` search returned 2 owned URLs (`ranchomoonrise.com/`, `ranchomoonrise.com/events/`) among 10 results, session not degraded, no banned copy in the returned snippets.
   - `site:ranchomoonrise.com safari tents` → same 8-owned-URL set as 8/31–9/11 (`/accommodations/`, `/`, `/blog/corporate-retreat-near-austin/`, `/host-your-event/`, `/corporate-retreats/`, `/weddings/`, `/faqs/`, `/blog/things-to-do-manor-tx/`). Page itself still never appears. By the established day-count (126 on 8/31, ~137 on 9/11), 9/12 is ~138 days.
   - Result: **still_true**. Per the standing 8/31 conclusion, no further on-site work queued.

2. **Claim: 5-file NEEDS OWNER uncommitted set unchanged.**
   - `git diff --stat` matches the baseline byte/line counts exactly (see above); `git log` confirms zero touching commits since baseline. No 6th file present.
   - Result: **still_true**.

`[2026-09-12 08:00] re-verify safari-tents-indexing — still_true — live=absent(8 owned URLs) prior=absent(8 owned URLs)`
`[2026-09-12 08:00] re-verify 5-file-needs-owner-set — still_true — live=5 files (byte-identical) prior=5 files`

## Site health

- `sitemap.xml` 200, `robots.txt` 200.
- All 30 sitemap URLs individually curled: **30/30 200**.
- `npm run validate:site` passes (0 invalid JSON-LD, VOICE-GUIDE checks clean, all internal assets resolve, sitemap↔vercel.json rewrites consistent).
- TTFB spot check, 6 routes: `/` 0.229s, `/weddings/` 0.318s, `/accommodations/` 0.291s, `/events/` 0.201s, `/faqs/` 0.329s, `/blog/` 0.206s — all in the normal 0.20-0.33s range, no regression.
- PageSpeed Insights API: same known quota-exhausted blocker family flagged repeatedly since 9/5 — not re-tested (no working key in this environment), not a new finding.

## On-page levers checked (new this run — not previously logged in run history)

`validate-site.py` checks internal asset (`src`/`data-src`/`srcset`) resolution but not `<a href>` link targets, and has no title/description duplication check. Both were new levers this run:

1. **Broken internal links.** Parsed every `<a href>` across the same 32-page public set the validator walks, resolved each internal (non-external/anchor/mailto/tel) href against the sitemap + `vercel.json` rewrite routes: **965 internal links checked, 599 external/anchor links skipped, 0 broken.**
2. **Duplicate `<title>` / meta description.** Extracted `<title>` and `<meta name="description">` from all 32 pages and grouped by exact text: **1 duplicate pair found**, both title and description — `site/pages/weddings.html` and `site/pages/weddings 2.html`. Confirmed via `diff` the two files are byte-identical, and the ` 2.html` copy is the known untracked, `.gitignore`d (`* [2-9].*` pattern), unreferenced file-sync duplicate logged since 8/31/9/10/9/11 — not a real duplicate-content defect, since it's never deployed or crawlable. No other duplicates across the 32-page set.

Both levers measured dry — nothing to fix, not skipped.

## Minor hygiene, not actioned

- `site/pages/weddings 2.html` — still present, untracked, byte-identical to `site/pages/weddings.html`, unreferenced anywhere in the codebase, `.gitignore`d. Same file-sync-duplicate family logged 8/31, 9/10, 9/11. Zero SEO/functional impact. Left alone.

## Outcome

Clean cruise-control run: repo started synced with origin (no stranded commits to land this time), no genuine on-page defect found, none manufactured. Two new levers checked for the first time in this task's run history (broken internal links, duplicate title/description) — both came back clean, consistent with the pattern of exhausted on-page levers noted in CONTEXT.md/TODO.md.

Gate: 2 claims, 2 still_true.
