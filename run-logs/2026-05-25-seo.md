# 2026-05-25 — rancho-site-daily run-log

## Workstream
`isAccessibleForFree: true` enrichment across the 17-post BlogPosting JSON-LD cluster.

## Pre-scoping source
CONTEXT.md 5/24 entry named `isAccessibleForFree: true` as the #1 next-slot candidate.

## Re-Verify Gate (live, before edits)
| Claim | Live state | Result |
|---|---|---|
| apex serves 200 from Vercel | `curl -I https://ranchomoonrise.com/` → 200 + `server: Vercel` + `x-vercel-cache: HIT` | still_true |
| www 308-redirects to apex | `curl -I https://www.ranchomoonrise.com/` → 308 + `location: https://ranchomoonrise.com/` | still_true |
| sitemap serves 200 | `curl -I https://ranchomoonrise.com/sitemap.xml` → 200 | still_true |
| `/corporate-retreats/` serves 200 | 200 | still_true |
| `/safari-tents-near-austin/` serves 200 | 200 | still_true |
| `/blog/` serves 200 | 200 | still_true |
| **CONTEXT claim: 0/17 BlogPosting carry `isAccessibleForFree`** | `grep` returned 1 match, but on `corporate-retreats.html` (EventVenue, not BlogPosting) | still_true (CONTEXT framing accurate) |

6/6 verified claims still_true; 0 stale claims auto-resolved.

## Edits
- 17 BlogPosting HTML pages in `site/pages/` — appended `"isAccessibleForFree": true` after `"inLanguage": "en-US"`; bumped `BlogPosting.dateModified` 2026-05-24 → 2026-05-25.
- `site/sitemap.xml` — 17 `<lastmod>` entries bumped to 2026-05-25.

## Validation
- Python `json.loads` on every BlogPosting block post-edit: all parse.
- Assertion per block: `obj["isAccessibleForFree"] is True` AND `obj["dateModified"] == "2026-05-25"` → 17/17 pass.
- `npm run validate:site` → Site validation passed.

## Diff scope
- 18 files staged, 85 insertions / 51 deletions.
- Pre-existing prior-session uncommitted changes in `api/inquiry.js`, `site/css/styles.css`, `site/js/main.js`, `site/pages/weddings.html`, `site/pages/contact.html` intentionally NOT staged (5/7–5/24 convention).

## Improvement-plan mapping
This work does NOT map to any of the 8 named SEO task IDs (s1–s8). `isAccessibleForFree` is post-level CreativeWork schema, distinct from s4 (Breadcrumb + Speakable). No done-log entry.

## Pre-scoping for next slot
`copyrightYear: 2026` + `copyrightHolder` (publisher.Organization reference) on BlogPosting JSON-LD — surgical, autonomous, cluster-wide; 0/17 currently carry it; rounds out the CreativeWork-property enrichment arc.

FLAG_FOR_ADAM: (none new — Hipcamp/ResortPass/Hotels.com/blog-pipeline carry from prior runs as documented in TODO.md "NEEDS ADAM")
