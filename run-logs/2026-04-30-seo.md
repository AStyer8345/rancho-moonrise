# SEO Daily Run — 2026-04-30

**Wall-clock start:** 2026-04-30 08:52 local
**Task:** rancho-site-daily

## Summary

🚀 **DNS cutover verified LIVE on Vercel** — the #1 SEO blocker that's been gating every workstream is RESOLVED. Auto-resolved via Re-Verify Gate.

Closed the AggregateRating coverage gap on the 2 remaining utility pages (`faqs.html` + `contact.html`) that was queued in CONTEXT.md as the next planned task. Coverage now 16 of 17 customer-facing pages (up from 14).

## Re-Verify Gate

| Claim | Verification | Outcome |
|---|---|---|
| `DNS still on Flywheel/5.1.0 (BofillTech)` (CONTEXT.md Active Blockers) | `curl -I https://ranchomoonrise.com/` | **resolved** — live=`server: Vercel, x-vercel-id: cle1::wl4qg-...`, prior=`x-fw-server: Flywheel/5.1.0` (RUN_2026-04-29) |
| `Vercel sitemap returns 200` | `curl -o /dev/null -w '%{http_code}' https://www.ranchomoonrise.com/sitemap.xml` | **still_true** — live=200 |
| `Production landing pages serve from Vercel` | `curl -I https://www.ranchomoonrise.com/pages/safari-tents-near-austin.html` | **still_true** — live=200, `server: Vercel` |
| `S2 AEO claims (aeo-block on key pages)` | not re-checked this run; no S2 edits today | n/a |
| `S4 BreadcrumbList + SpeakableSpec on key pages` | extended (added LocalBusiness w/ AggregateRating to 2 pages) | still_true |

`[2026-04-30 08:55] re-verify dns-flywheel-bofilltech — resolved — live=Vercel prior=Flywheel/5.1.0`

## Files changed

- `site/pages/contact.html` — extended nested `LocalBusiness` (under `mainEntity` of `ContactPage`) with `aggregateRating` 4.9 / 125 / bestRating 5
- `site/pages/faqs.html` — added stand-alone `LocalBusiness` JSON-LD block with `aggregateRating` (FAQPage has no `publisher` slot to nest into, so stand-alone is the right shape)
- `site/sitemap.xml` — bumped lastmod for `/contact/` (04-07 → 04-30) and `/faqs/` (04-08 → 04-30)
- `rancho-done-log.md` — appended RESOLVED entry for `rancho-p2-11-website-launch-dns` + PROGRESS 95% for `rancho-seo-s4-schema-breadcrumb-speakable`
- `CONTEXT.md` / `CHANGELOG.md` / `TODO.md` — four-file system updates

## Validation

All 7 JSON-LD blocks across the 2 edited pages parse via `python3 json.loads`:
- contact.html: 3 blocks (ContactPage, BreadcrumbList, WebPage)
- faqs.html: 4 blocks (FAQPage, BreadcrumbList, LocalBusiness [new], WebPage)

## NEEDS ADAM (post-cutover, surfaced today)

1. **Canonical strategy decision (apex vs www).** All 17 page canonicals + all 27 sitemap URLs use apex (`https://ranchomoonrise.com/...`) but apex 307s to www. The canonical-vs-served-host mismatch is suboptimal. Two clean paths:
   - **(A) Set apex as primary domain in Vercel** — no code change; redirects flip to www → apex, matches existing canonicals. **Recommended.**
   - **(B) Rewrite all canonicals + sitemap URLs to www** — bigger code change; let www be primary.
2. **GSC sitemap submit (rancho-seo-s6).** With DNS live, submit `/sitemap.xml` to Search Console. Was blocked on cutover; now actionable. Defer until canonical decision.

## Improvement Plan task progress

- `rancho-p2-11-website-launch-dns` — **RESOLVED** (auto, via Re-Verify Gate)
- `rancho-seo-s4-schema-breadcrumb-speakable` — PROGRESS 95% (16 of 17 customer-facing pages now carry AggregateRating)

## Skills note

PreToolUse hook injected Next.js skill recommendations on `site/pages/*.html` reads/edits — ignored, false positive (this is a static HTML site, no Next.js, no Tailwind, no `next/font`, no React). PostToolUse validation suggested migrating external font loaders to `next/font` — same false positive, ignored.
