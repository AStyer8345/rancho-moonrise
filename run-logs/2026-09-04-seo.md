# rancho-site-daily — 2026-09-04

**Headline: WeddingWire's `sameAs` contradiction (open since 2026-08-19) is resolved, and today's assigned "extend AEO measurement to a second answer engine" slot came back negative — with a finding worth keeping.**

---

## 1. Site health — Re-Verify Gate

Brand canary first (harness `WebSearch`): query `Rancho Moonrise` returns `ranchomoonrise.com` alongside Do512/Hotels.com/LinkedIn/The Knot/Facebook/Instagram/Hipcamp. Session not degraded.

| Claim | Live check | Result |
|---|---|---|
| Sitemap/robots/home return 200 | `curl -s -o /dev/null -w "%{http_code}"` | 200 / 200 / 200, TTFB 0.20–0.31s |
| `sameAs` carries 14 URLs (8/31 claim) | live `curl` + JSON-LD parse | **14**, exact match |
| `/safari-tents-near-austin/` still uncrawled, closed on-site side (carried since 8/25, "do not queue further on-site work") | `site:ranchomoonrise.com safari tents` | still absent from 8 returned owned URLs (same set as 8/31) — **still_true**, not re-diagnosed, per standing instruction |
| 5 files uncommitted, NEEDS OWNER (flagged 8/31) | `git status --short` | unchanged: `api/inquiry.js`, `site/css/styles.css`, `site/js/main.js`, `site/pages/contact.html`, `site/pages/weddings.html` — **still_true**, not this task's to land |

`[2026-09-04 08:45] re-verify sameas-14 — still_true — live=14 prior=14`
`[2026-09-04 08:45] re-verify safari-tents-uncrawled — still_true — live=absent prior=absent`
`[2026-09-04 08:45] re-verify uncommitted-5-files — still_true — live=5 prior=5`

## 2. WeddingWire `sameAs` candidate — RESOLVED

Carried as an open contradiction since 2026-08-19 ("The repo contradicts itself on WeddingWire — worth 60 seconds to settle either way") and excluded again 8/25/8/31 for lack of independent confirmation.

Resolved today **by varying the query, not attempting a direct fetch** (WeddingWire is a known 403 on both `curl` and `WebFetch` — retrying that proves nothing, same discipline used for Expedia and the GBP CID):

- Query `Rancho Moonrise weddingwire.com` returned `https://www.weddingwire.com/biz/rancho-moonrise/f38c5e35e5491216.html`
- Independent second query `"Rancho Moonrise" Manor TX f38c5e35e5491216` returned the identical URL again, plus a model-read confirmation that the ID string is WeddingWire's own listing ID
- Cross-checked against the property's operational address, **20117 Lockwood Rd, Manor, TX 78653** (from `CONTEXT.md`) — the WeddingWire result's own summary names the same address

Two-factor confirmation (independent query + address match), same bar used for the Expedia and GBP entity additions. Added to `site/index.html`'s `sameAs` array (14 → **15**). `npm run validate:site` passes.

## 3. Today's assigned top-autonomous-slot: extend AEO measurement to a second answer engine — attempted, came back negative, and the negative result matters

CONTEXT.md named this the top open item: the 4/10 answer-engine-citation figure from `serp-baseline-2026-08-18.md` rests on exactly one engine ("this harness's own synthesis"). ChatGPT/Perplexity/Google AI Overview need paid keys and stayed out of scope. Tried the cheapest available thing first: **does `WebFetch` succeed where bare `curl` failed**, on Bing and Google directly, since `WebFetch` is a genuinely different fetch mechanism (renders + AI-summarizes) than the bare-curl paths already marked DEAD in the shared runbook.

- `WebFetch` on `bing.com/search?q=Rancho+Moonrise+Manor+TX` → brand canary **FAILED**. Returned `gorancho.com`, `en.wikipedia.org/wiki/Rancho`, `merriam-webster.com/dictionary/rancho` — the *identical* failure signature the 8/17 bare-curl attempt got on the same query. Per the shared runbook's mandatory guard, this is void and **not recorded as "0 citations."**
- `WebFetch` on `google.com/search?q=Rancho+Moonrise+Manor+TX` → returned an error/interstitial page, zero results rendered.

**Conclusion: this closes the "maybe a different fetch mechanism unlocks a second engine" hypothesis definitively.** It isn't a bare-curl artifact — `WebFetch` is bot-detected on both Bing and Google the same way. True second-engine AEO measurement genuinely requires a paid API (ChatGPT, Perplexity, Bing Web Search API, or Google AI Overview access), exactly as CONTEXT.md already suspected but hadn't tested. Recording this so no future run re-attempts `WebFetch` as a workaround.

**Extending the shared "Known-dead verification paths" table** (`client-ops/templates/re-verify-before-report.md`) with this finding — it's a fleet-wide fact, not Rancho-specific.

## 4. Apple Maps entity re-check — cross-verification + one new finding

Re-read `https://maps.apple.com/place?place-id=IECFC57C438E79EC4` via `WebFetch` (a genuinely different tool path than the `curl` + Googlebot-UA method used elsewhere on this property).

- **Cross-verifies `rancho-review-monitor`'s 8/30 finding independently**: 80% overall, 5 ratings — exact match, second tool, second read, same value. Not promoting or touching review numbers — that's review-monitor's remit, noted here only as corroboration.
- **New finding, not review-count/reply-state, so within this task's remit**: Apple's listed hours read **"3:00 PM – 5:00 PM daily (currently closed)"**. That is almost certainly wrong or badly incomplete for an events/glamping venue — a 2-hour daily window reads like a stale or default value, not real operating hours. If Siri/Spotlight answers "is Rancho Moonrise open" or "when is Rancho Moonrise open" using this data, it will tell people the property is closed nearly all day, every day. This is an **Apple Business Connect** correction, which this task cannot make (no account access) — flagged NEEDS ADAM below, same pattern as the GBP Q&A/Chat/photos asks.

## 5. Banned-copy propagation — observed again, not actioned (not this task's remit)

Both brand-canary answers synthesized today repeated the banned third-party description — *"20 luxury cabins and safari tents for up to 50 guests"* — matching the exact sentence `rancho-competitive-weekly` traced to The Knot on 8/19/8/25/8/31. Third-plus confirmation, noted here only because it surfaced organically; ownership and the fix-at-source ask already live in `TODO.md` under competitive-weekly's item.

---

## Gate summary

4 claims re-verified (3 still_true, 1 resolved this run: WeddingWire). 1 new finding (Apple Maps hours). 1 negative-but-useful finding (WebFetch-Bing/Google both dead, extends shared runbook). 0 site copy changed on public pages. `validate:site` passes.
