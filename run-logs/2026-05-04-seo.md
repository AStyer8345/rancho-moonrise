# 2026-05-04 — rancho-site-daily run log

## Goal

Run daily SEO/AEO improvement pass. Pick a concrete improvement that doesn't depend on Adam-only inputs (GSC submit, ResortPass decision, Glamping Hub submit, GA4 ID, low-res JPG re-upload, etc.).

## What I checked first

- Read CLAUDE.md, GOALS.md, CONTEXT.md, recent CHANGELOG and rancho-done-log.
- Re-Verify Gate against the live site:
  - apex `https://ranchomoonrise.com/` → 200, `server: Vercel`, `x-vercel-cache: HIT` — DNS still on Vercel, claim still_true
  - www → 308 to apex — canonical redirect still_true
  - `/sitemap.xml`, `/corporate-retreats/`, `/safari-tents-near-austin/`, `/events/`, `/weddings/`, `/faqs/` all 200 — landing pages all serving
  - `/robots.txt` sane (apex sitemap reference, internal admin/dashboard pages disallowed)
  - All DNS/canonical/sitemap claims still_true. **No stale claims auto-resolved this run.**

## What I picked + why

S4 schema-breadcrumb-speakable hit 17/17 yesterday (`/events/` AggregateRating closure). The next bottleneck is "Google has not yet crawled apex" — that's owned by Adam (GSC sitemap submit). What I *can* do autonomously today is raise SERP CTR for the eventual reindex.

Audited every customer-facing page's `<meta name="description">` against Google's ~160-char desktop truncation window. Found 18 over the cutoff:
- 5 nav/landing pages: `/`, `/weddings/`, `/events/`, `/corporate-retreats/`, `/blog/pool-day-pass-austin/` (range 175–197 raw)
- 13 blog pages (range 166–210 raw)

(Also checked: `host-your-event.html` 158, `accommodations.html` 147, `safari-tents-near-austin.html` 163, `contact.html` 138, `faqs.html` 171. The faqs page at 171 is borderline but the front-loaded keyword + Austin anchor is in the first 130 chars, so truncation doesn't hurt. Left alone.)

## Edits

18 `<meta name="description">` rewrites. All now 129–155 chars rendered. Voice rules enforced (no `luxury`, no `Hill Country`, no `Manor, TX` in body copy, no specific overnight count, no `General Store`). Keyword-leading + Austin/20-minutes anchor preserved on every page where it was already there.

**Bonus voice cleanup:** `/blog/ranch-wedding-texas/` was using "Rancho Moonrise in Manor, TX" in its meta description — that's a SERP-visible body-copy violation per Ashley's directive (Manor, TX = schema/alt only). Rewrite drops the Manor reference.

**Skipped:** OG and Twitter descriptions on every edited page. They're already separate, distinct strings (verified via grep on `index.html`); truncation thresholds differ (OG no hard cutoff, Twitter ~200). Editing them risked breaking existing share-card framing for no SERP benefit.

## Sitemap

18 lastmod entries bumped to 2026-05-04 to nudge re-crawl when Google next visits the apex sitemap (which it will, post-GSC-submit).

## Validation

- `npm run validate:site` → passes
- Diff scope: 19 files, 36 insertions, 36 deletions — surgical
- Per-page rendered description length verified post-edit: 129–155 chars across all 18

## Improvement-plan task mapping

`rancho-seo-s7-aeo-baseline` — PROGRESS 40%. CTR groundwork is preparation for the AEO baseline; querying AI engines for the 10 target keywords still pending GSC indexing of apex.

## Re-verify ledger (live)

- [2026-05-04 08:15] re-verify dns-on-vercel — still_true — live=`server: Vercel` prior=`server: Vercel`
- [2026-05-04 08:15] re-verify www-308-to-apex — still_true — live=308→apex prior=308→apex
- [2026-05-04 08:15] re-verify sitemap-200 — still_true — live=200 prior=200
- [2026-05-04 08:15] re-verify corporate-retreats-200 — still_true — live=200 prior=200
- [2026-05-04 08:15] re-verify safari-tents-200 — still_true — live=200 prior=200
- [2026-05-04 08:15] re-verify events-200 — still_true — live=200 prior=200
- [2026-05-04 08:15] re-verify weddings-200 — still_true — live=200 prior=200
- [2026-05-04 08:15] re-verify faqs-200 — still_true — live=200 prior=200

No stale claims auto-resolved.

## What's next

Tomorrow's run options (in priority order):
1. **Title tag audit** — same CTR motivation. Check titles for length (≤60 chars sweet spot, ≤70 hard cap) and keyword leading. Probably less leverage than meta descriptions but worth a sweep.
2. **AEO baseline (s7) — partial start.** Live SERP queries for the 10 target keywords via DuckDuckGo HTML to capture pre-indexing baseline. Currently zero rankings; want to track when and how that changes post-GSC-submit.
3. **Internal linking audit on `/safari-tents-near-austin/`** — that page only has 0–1 internal inbound links. After /corporate-retreats/'s 7-source pass on 2026-05-02, a similar pass on safari-tents would close the cluster.
4. **May 29 Free Friday rotation** — not yet (event is 25 days out).

NEEDS ADAM (no autonomous lever): GSC sitemap submit, Glamping Hub listing, ResortPass decision, GA4 measurement ID, low-res JPG re-upload (8 files).
