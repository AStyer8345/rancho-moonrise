# rancho-site-daily — 2026-09-20

## Repo state at start — **main checkout is mid-rebase (NOT this run's doing, deliberately not touched)**

- `/Users/adamstyer/Documents/rancho-moonrise` is on a **detached HEAD in an interrupted `git rebase`** (`git status` → "HEAD (no branch, rebasing main)"). Last touched 2026-09-18 10:38; nothing running now. Reflog: `rebase (start): checkout origin/main` at 07:38 on 9/18, 8 local commits (7 notebooklm-sync + review-monitor RUN_078) replayed onto `dcd1a54`, `git-rebase-todo` empty, finalization never ran. The real `main` ref still sits at `ab1b210`, which is **behind `origin/main` by 10+ commits and ahead by 8 unpushed commits**.
- The notebooklm-sync task recorded the same anomaly 9/18 10:36 and correctly declined to run `--continue`/`--abort` (its rule). I followed the same conservative rule: no `rebase --continue/--abort`, no reset, no stash pop in that checkout.
- **The standing 5-file NEEDS OWNER set is no longer in the working tree — it's in `stash@{0}`** ("On main: pre-existing NEEDS OWNER uncommitted set (not review-monitor's)", created 2026-09-18 07:38:43). `git stash show --stat` matches the 8/31 baseline byte-for-byte: `api/inquiry.js` Bin 6182→6214, `site/css/styles.css` +114, `site/js/main.js` +58, `site/pages/contact.html` +11, `site/pages/weddings.html` −44 (plus a +1 to `run-logs/notebooklm-sync.md`, an auto-sync artifact). Read-only inspection only; nothing applied or dropped.
- Working tree of the main checkout also has one dirty file, `run-logs/notebooklm-sync.md` (+8, the 9/18 10:36 sync entry).
- **Workaround used:** `git worktree add --detach ../rancho-moonrise-daily-20260920 origin/main` (HEAD `986faba`), all work done and pushed from there. `origin/main` moved during the run (`9ace7ed` "footer-linked Adam Styer website contributor bio", another writer) — rebased over it, no conflict.
- The 8 stranded local commits (incl. `ab1b210` RUN_078 review-monitor) remain **unpushed to origin**. Not landed by this run: they live inside the broken rebase and touch files (`CONTEXT.md`, `run-logs/notebooklm-sync.md`) other writers own. Flagged in TODO.md.

## Re-Verify Gate

1. **Claim: `/safari-tents-near-austin/` absent from Google's index (on-site diagnosis closed 8/31).**
   - Brand canary first: `Rancho Moonrise glamping events Manor TX` returned `ranchomoonrise.com` (/, /contact/, /blog/, /events/, /blog/things-to-do-manor-tx/) — session not degraded.
   - `site:ranchomoonrise.com safari tents` → 9 owned URLs (`/accommodations/`, `/faqs/`, `/corporate-retreats/`, `/blog/corporate-retreat-near-austin/`, `/host-your-event/`, `/weddings/`, `/`, `/blog/things-to-do-manor-tx/`, `/blog/glamping-vs-camping/`). Target page absent. ~146 days by the established count.
   - Result: **still_true**. No further on-site work queued (standing 8/31 conclusion).
2. **Claim: 5-file NEEDS OWNER set sits uncommitted in the working tree.**
   - Live: not in the working tree; intact in `stash@{0}` with byte-identical stats.
   - Result: **partial** — content unchanged, *location* changed (working tree → stash). Docs updated.
3. **Claim: sitemap.xml healthy / all routes 200.** `sitemap.xml` 200; 30/30 URLs 200 at start, now **31** (another writer's `/website-credits/`, 200). **still_true**.
4. **Claim: schema present on every page.** All 30 sitemap pages carry ≥1 parseable JSON-LD block, 0 parse errors. **still_true**.

`[2026-09-20 13:20] re-verify safari-tents-indexing — still_true — live=absent(9 owned URLs) prior=absent(8 owned URLs)`
`[2026-09-20 13:20] re-verify 5-file-needs-owner-set — partial — live=5 files intact in stash@{0} (not in working tree) prior=5 files uncommitted in working tree`
`[2026-09-20 13:20] re-verify sitemap-routes-200 — still_true — live=31/31 (was 30/30) prior=30/30`
`[2026-09-20 13:20] re-verify schema-jsonld-all-pages — still_true — live=30/30 pages parse clean prior=30/30`

Not this task's (skipped, owned by review-monitor / gbp-weekly): review count, review reply state, GBP post cadence.

## Site health

- `sitemap.xml` 200. 30/30 URLs 200 at run start; 31/31 after deploy. `validate:site` passes. `server: Vercel`.
- TTFB spot-check post-deploy: `/blog/` 0.24s, `/policies/` 0.19s.
- PageSpeed API not re-attempted (same quota blocker family as 9/12); the 9/16 audit recorded live mobile Lighthouse 99/100/100/100 (one lab pair) — a stronger baseline than any prior run had.

## Lever sweep (live, all 30 pages, real HTML parser)

New: the site changed materially on 9/16 (audit) and 9/19–20 (homepage event artwork, footer credits), so re-swept every lever on live HTML rather than trust the 9/10–9/12 clean results.

| Lever | Result |
|---|---|
| Meta description length | 30/30 within 70–160 (longest `host-your-event` 158). *(My first regex pass flagged 3 pages at 6–40 chars — parser artifact from apostrophes in content; re-parsed with `html.parser`, all fine. Recorded because it's the same class of false positive this task has burned on before.)* |
| Title length | 4 of 30 over 65: `/faqs/` 67, `/blog/` 66, `/policies/` 66, `/blog/ranch-wedding-texas/` 68. Brand suffix is the tail so truncation costs little; Google rewrites titles anyway. **Not changed** — marginal, no measured benefit. |
| Canonical = URL | 30/30 |
| Exactly one H1 | 30/30 |
| `noindex` | 0 |
| Duplicate title / description | 0 |
| `<img>` without alt | 0 |
| JSON-LD parse errors | 0 |
| **`og:image` present** | **28/30 — `/blog/` and `/policies/` had no OG/Twitter block at all** ← fixed |

## Shipped: Open Graph + Twitter tags on `/blog/` and `/policies/`

- Commit `2f30443` on `origin/main`. `site/pages/blog.html` +10, `site/pages/policies.html` +10. Reuses each page's existing `<title>` and meta description verbatim (no new copy → no VOICE-GUIDE exposure) and the site-default `og-image.jpg` (16 pages already use it; serves 200, 875,624 bytes). Omitted `og:image:width/height` deliberately — that image is 2134×1412, not 1200×630, and I won't declare dimensions that aren't true.
- Why it matters: any shared `/blog/` or `/policies/` link (text, DM, social) previously had no image to render in its preview. It's a share-preview defect, **not** a ranking lever — stating that plainly so it isn't read as SEO impact.
- Destination verification: `git show origin/main:site/pages/{blog,policies}.html` → 1 `og:image` each; live URLs (cache-busted) served the tags on the 3rd 15-second poll (roughly 30–45s after push); `/blog/` still returns 2 JSON-LD blocks, 200, TTFB 0.24s. Vercel deployment `dpl_AV1SA1t1fCucU5j9eEf4NbM1FdH7` (production, commit `2f30443885973bb95d272ca71ef28addc7915bbe`) read **READY** via `list_deployments` (first attempt with the 7-char SHA returned empty — full SHA required).

## Improvement-plan mapping

None of today's work maps to a listed task ID (`rancho-seo-s1`…`s8`, `p2-11`, `p3-20`). `rancho-seo-s6` (GSC/indexing) is unchanged and still owner-gated on URL Inspection for the safari-tents URL. No done-log lines written; no PROGRESS claimed.

## Flags

- **NEEDS ADAM / owner:** main checkout stuck mid-rebase since 9/18; 8 commits unpushed; 5-file set now in `stash@{0}`. See TODO.md.
- Still open, unchanged: banned Knot/Hotels.com syndicated line ("luxury cabins and safari tents … up to 50 guests", "34 acres") echoed again in today's brand-canary answer — existing NEEDS ADAM item, not new.
