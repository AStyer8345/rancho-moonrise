# rancho-site-daily — 2026-08-16

**Status:** shipped edit (3rd consecutive shipping run)
**Pause gate:** clear — GOALS.md lists `rancho-site-daily` under "Keep running"; site/content work RESUMED 2026-07-15 (outreach-only pause).

## Coverage gap

**No site-daily run between 2026-07-24 and today — 23 days.** Last SEO run-log was `2026-07-23-seo.md` (itself left uncommitted); the only repo activity since was `notebooklm-sync` commits, most of them auth-blocked. Not reconstructable. The 7/23 run-log is committed with this run so it stops being an orphaned artifact for already-shipped commit `089439f`.

## Re-Verify Gate

The entire pre-scoped "next slot" queue carried in CONTEXT.md turned out to be **already shipped** — by two commits that landed after the last CONTEXT.md rewrite:

| Claim (from CONTEXT.md "Next SEO run") | Live/repo result |
|---|---|
| "`videos.html` carries no JSON-LD at all — highest-value remaining slot" | **RESOLVED** — 3 blocks live (CollectionPage + BreadcrumbList + ItemList of 3 VideoObject), shipped `089439f` 2026-07-23 |
| "`accessibility.html` (no schema at all) + `policies.html` (BreadcrumbList only) lack WebPage/speakable" | **RESOLVED** — both carry BreadcrumbList + WebPage w/ speakable, shipped `531d7a5` 2026-07-24 |
| apex 200 + `server: Vercel` + `x-vercel-cache: HIT` | still_true (`age: 57231` ≈ 15.9 hr) |
| www → apex 308 | still_true |
| sitemap.xml / robots.txt / videos / accessibility / policies / corporate-retreats / safari-tents 200 | still_true (7/7) |

**Sitewide re-audit (29 public pages, `weddings 2.html` excluded as untracked junk):**
- **0 pages with invalid JSON-LD**
- **0 pages without `speakable`** — coverage is now total
- Sitemap ↔ routes ↔ page-files coverage: **29 routes / 30 sitemap entries (29 + apex) / 29 page files, zero gaps in all three directions** (nothing routed-but-unlisted, nothing listed-but-unrouted, no page file without a route)

Two false alarms caught and killed before they reached this log, both mine:
- `pool-day-pass-austin.html` first appeared to carry invalid JSON-LD — actually my audit script mishandled `"@type": ["WebPage","ReservePage"]` (a list, not a string). Page is fine.
- `/pool-day-pass-austin/` appeared to 404 — the real canonical path is `/blog/pool-day-pass-austin/`, which returns 200 and is correctly in the sitemap.

## Shipped

**Internal-linking floor restoration** — `ranch-wedding-texas.html` was the single thinnest-linked post in the 17-post blog cluster at **3 inbound topical links**, against a floor of 4 held by every other post since the 2026-05-10 close-out.

Checked against `HEAD` before writing: the inbound set is byte-identical in the working tree and at `HEAD`, so **this is not a regression** — the post has simply always sat at 3 under this counting method. Reported as a gap, not as drift.

- Added one `<li>` to the Related Reading list in `bachelorette-party-austin-texas.html` → `/blog/ranch-wedding-texas/`.
- Host chosen for link equity: bachelorette is one of the better-linked posts in the cluster (8 inbound), and it already carries a `/blog/wedding-venues-near-austin/` link, so the bachelorette→wedding topical adjacency was already established editorially. Placed directly after that line to keep wedding-topic links adjacent.
- Anchor text uses the target's exact `<title>`/`<h1>`. The other three inbound links use three *different* phrasings; that variance is deliberate and preserved — uniform exact-match anchors across every inbound link is an over-optimization footprint.
- `BlogPosting.dateModified` 2026-05-26 → 2026-08-16 on the edited page; sitemap `lastmod` synced for that one URL via an anchored regex asserting exactly 1 replacement.

**Result: blog-cluster inbound floor is a uniform 4 across all 17 posts.**

2 files, +1 line / 2 date bumps. Additive-only, no copy changes.

## Verified

- `npm run validate:site` → **passes**
- 4/4 JSON-LD blocks on the edited page re-parse
- Cluster floor recomputed post-edit: **4** (was 3); `ranch-wedding-texas` inbound 3 → 4
- `site/sitemap.xml` re-parsed well-formed via `xml.dom.minidom`
- Sitemap `lastmod` replacement count asserted `== 1`

## FLAG_FOR_ADAM

- **Dead YouTube video still live on `/videos/`, day 24.** Card "Escape to Rancho Moonrise" (`_klefu2vTwM`) — re-verified today: thumbnail **404**, oembed **403**. The other three video IDs on the page all return 200/200. The card renders a broken thumbnail to real visitors. First flagged 2026-07-23; still unactioned. Either re-upload the video or delete the card — 30 seconds once decided. I've left the card in place rather than silently deleting visible content, but this is now the oldest live user-facing defect on the site.

## Notes / not touched

- **Untracked duplicate files are accumulating** in the repo working tree — `weddings 2.html`, `CONTEXT 2.md`/`3.md`, `TODO 2/3/4.md`, `CHANGELOG 2/3/4.md`, `AGENTS 2.md`, `styles 2.css`, several `run-logs/... 2.md`. All **untracked**, so none of them deploy (Vercel builds from git) — not a live risk. Almost certainly Finder/iCloud-style copy artifacts from concurrent sessions. Left alone; flagged so someone can sweep them deliberately.
- Pre-existing uncommitted prior-session changes (`api/inquiry.js`, `site/css/styles.css`, `site/js/main.js`, `blog.html`, `contact.html`, `videos.html`, `wedding-venues-near-austin.html`, `weddings.html`) **intentionally NOT staged**, per the standing convention. Committed by explicit path only. Note `videos.html` carries an uncommitted `speakable` addition to its CollectionPage block from another session — left for that session to land.
- Indexing/SERP → `rancho-competitive-weekly` + Adam (GSC). Review/GBP → `rancho-review-monitor` / `rancho-gbp-weekly`. Neither re-litigated here.

## Next slots (remaining surgical runway)

The schema runway really is exhausted this time — validity, `speakable`, breadcrumbs, sitemap/route coverage are all at 100%. Remaining autonomous work is no longer schema-shaped:

1. **AEO baseline (`rancho-seo-s7`)** — query AI engines for the 10 target keywords, establish the tracking baseline. Still gated on GSC crawl of the two lagging pages, but the *measurement* half can run regardless and is the highest-value remaining slot.
2. **Deepen cluster floor 4 → 5** inbound topical links — mechanical, low-risk, same pattern as today.
3. Promote `/corporate-retreats/` into main nav (footer-only today) — BLOCKED on Adam confirm.
