# rancho-site-daily — 2026-09-25

## Repo state at start
- Main checkout `/Users/adamstyer/Documents/rancho-moonrise` still detached mid-rebase ("HEAD (no branch)", dirty `run-logs/notebooklm-sync.md`). Not touched.
- Worked from `git worktree add --detach ../rancho-moonrise-daily-20260925 origin/main` (HEAD `e71db0c`).
- Production check before push: `vercel inspect ranchomoonrise.com` → `dpl_9rX7unTtjpg4mSnGygfd2WKNSd1d`, a git build (astyer8345, 9/24 18:15 CT). The 9/24 CLI pin is gone, so a push goes live normally.

## Re-Verify Gate
1. **Brand canary** `Rancho Moonrise glamping events Manor TX` → 5 owned URLs (/contact/, /blog/, /blog/things-to-do-manor-tx/, /events/, /). Session healthy. Answer text again echoed the banned "luxury cabins and safari tents … up to 50 guests / 34 acres" copy (off-domain, existing NEEDS ADAM item).
2. **`/safari-tents-near-austin/` absent from index.** `site:ranchomoonrise.com safari tents` → 9 owned URLs, same set as 9/21 and 9/24; target absent. **still_true** (owner URL Inspection is the only lever).
3. **Sitemap routes.** Live `sitemap.xml` 31/31 200. **still_true.**
4. **Dead YouTube card on `/videos/` (`_klefu2vTwM`).** Still on page (2 refs); thumb 404, oembed 403. **still_true, ~day 64.**
5. **`/host-your-event/` "1,000+ guests"** → 2 live occurrences. **still_true** (owner answer needed).
6. **Main checkout mid-rebase.** **still_true.**

`[2026-09-25 11:25] re-verify safari-tents-indexing — still_true — live=absent(9 owned URLs) prior=absent(9 owned URLs)`
`[2026-09-25 11:25] re-verify sitemap-routes-200 — still_true — live=31/31 prior=31/31`
`[2026-09-25 11:25] re-verify videos-dead-embed — still_true — live=thumb 404 / oembed 403 prior=same`
`[2026-09-25 11:25] re-verify host-your-event-1000-guests — still_true — live=2 occurrences prior=2`
`[2026-09-25 11:25] re-verify main-checkout-rebase — still_true — live=detached rebasing prior=same`

Not this task's (skipped): review count / reply state / GBP cadence.

## Finding
`/offthegrid/` (Off the Grid day retreat, Sat Nov 14 2026, with Swift Fit Social) went into git 9/24 as a recovered CLI page. It's the only upcoming event on the site and is linked from the header, but:
- no canonical, no OG/Twitter tags (shared links had no preview image or card)
- no Event or BreadcrumbList JSON-LD (the only public page without breadcrumb; not eligible for Google event results)
- not in `sitemap.xml`, and its only internal link is injected by `js/main.js` (JS-only discovery)
- `site:ranchomoonrise.com "Off the Grid" retreat November` → not indexed yet (baseline, day 2 in git)

## Shipped
- `site/offthegrid/index.html`: canonical; OG + Twitter `summary_large_image` tags; `Event` JSON-LD (start 2026-11-14T12:00-06:00, end 18:00 — CST after DST ends; Offline; Scheduled; Place + PostalAddress matching sibling schema; organizers Rancho Moonrise + Swift Fit Social; offer URL = the page's Square checkout link); `BreadcrumbList` Home > Off the Grid.
- Deliberately **no price or availability** in the Offer. The Square page renders price with JS and the page never states one. Google treats both as recommended, not required.
- New `site/offthegrid/images/og-off-the-grid.jpg`: a 1200×630 crop of the page's own hero (196 KB), matching the sibling og:image size.
- `site/sitemap.xml`: added `/offthegrid/` (lastmod 2026-09-25, weekly, 0.7). Sitemap now 32 URLs.
- `scripts/validate-site.py`: the sitemap check required a `vercel.json` rewrite for every path, but `/offthegrid/` is a static directory page (`site/offthegrid/index.html`) and needs none. It now accepts a path that has an `index.html` on disk. Negative test: a bogus `/nope/` sitemap path still fails.
- `validate:site` passes; `test:runtime` passes.

## Not done / noted
- `images/hero-pool.png` is a 3.0 MB PNG used as the page's CSS hero background, so it's likely the LCP element. Converting it to WebP is a clear perf win. Left for its own run: one improvement per day, and it's another writer's asset. Added to TODO.
- `/events/` still has 4 Event JSON-LD blocks, all past-dated (May–July 2026), and no link to Off the Grid. Added to TODO. I didn't touch it because events there may be CRM-loaded; check the loader first.
- PageSpeed API quota (known blocker family) not retried; the 9/16 Lighthouse 99/100/100/100 is still the latest measurement.
