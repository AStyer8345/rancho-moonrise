# rancho-site-daily — 2026-09-21

## Repo state at start
- Main checkout `/Users/adamstyer/Documents/rancho-moonrise` is **still** a detached HEAD in the interrupted rebase from 9/18 (`git status` → "HEAD (no branch, rebasing main)", one dirty file `run-logs/notebooklm-sync.md`). Not touched — same conservative rule as 9/20 (no `--continue`/`--abort`, no stash pop).
- Worked from `git worktree add --detach ../rancho-moonrise-daily-20260921 origin/main` (HEAD `f824d70`, review-monitor RUN_079).
- `stash@{0}` (5-file NEEDS OWNER set) still present, untouched.

## Re-Verify Gate
1. **`/safari-tents-near-austin/` absent from index.** Brand canary `Rancho Moonrise glamping events Manor TX` returned owned URLs (/contact/, /blog/, /events/, /, /blog/things-to-do-manor-tx/) — session healthy. `site:ranchomoonrise.com safari tents` → 9 owned URLs (accommodations, faqs, corporate-retreats, blog/corporate-retreat-near-austin, host-your-event, weddings, /, blog/things-to-do-manor-tx, blog/glamping-vs-camping); target absent. **still_true.**
2. **Sitemap / routes 200.** `sitemap.xml` 200, `server: Vercel`; 31/31 URLs 200, none >0.6s TTFB. **still_true.**
3. **TODO "Schema gaps" #1–#3 (videos, accessibility, policies).** Live JSON-LD matrix over all 31 pages (real JSON parse): every page has BreadcrumbList (except homepage, by design) + a WebPage-family type + `speakable`; 0 parse errors. **resolved** — stale since 8/16.
4. **"RUN_078 commit isn't on `origin/main`"** (sub-claim of the 9/20 mid-rebase blocker). `git log origin/main --grep=RUN_078` → `1ffb25a`. **resolved** (only the 7 notebooklm-sync commits are truly stranded).
5. **Main checkout mid-rebase.** Live `git status -sb` → still rebasing. **still_true.**

`[2026-09-21 09:20] re-verify safari-tents-indexing — still_true — live=absent(9 owned URLs) prior=absent(9 owned URLs)`
`[2026-09-21 09:20] re-verify sitemap-routes-200 — still_true — live=31/31 prior=31/31`
`[2026-09-21 09:20] re-verify schema-gaps-videos-accessibility-policies — resolved — live=all 31 pages have breadcrumb+WebPage+speakable prior=3 pages flagged (TODO, 7/22)`
`[2026-09-21 09:20] re-verify run078-not-on-origin — resolved — live=on origin/main as 1ffb25a prior=not on origin/main`
`[2026-09-21 09:20] re-verify main-checkout-mid-rebase — still_true — live=HEAD (no branch, rebasing main) prior=same`

Not this task's (skipped): review count / reply state / GBP cadence.

## Shipped
`BreadcrumbList` on `/website-credits/`. It was the only page (of 31) missing one; the page arrived 9/19–20 from another writer with a WebPage block only. Same shape as `/policies/`. No `speakable`: a credits page isn't voice-answer content, and CONTEXT says avoid schema work without a guest-facing or measured benefit — this one is justified only as sitewide consistency, and I'm saying so plainly. Not a ranking lever.
Checks: 2 JSON-LD blocks parse (WebPage, BreadcrumbList); `npm run validate:site` passes.

## Lever rejected
Sitemap `lastmod` vs git: 30/31 read "stale" by git date, but the 9/19–20 footer-link commit (`9ace7ed`) touched every page, so git date ≠ content date. Bumping all `lastmod`s would signal freshness that doesn't exist. Left as is.

## Still open (unchanged, existing owner items)
- `/host-your-event/` FAQ says "up to 1,000+ guests" (visible + FAQPage schema) beside Barn = 200 seated and `maximumAttendeeCapacity: 200`. Verified live today (2 occurrences). Needs an owner answer; not changed.
- Banned Knot/Hotels.com syndicated line ("luxury cabins and safari tents … up to 50 guests", "34 acres") echoed again in today's brand-canary answer — existing NEEDS ADAM item.
